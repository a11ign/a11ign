# NO ENTRY IN THIS FILE ENDS IN A SLASH, and that is load-bearing rather than a style choice (#1983).
# A trailing slash means DIRECTORY, and a SYMLINK IS NOT A DIRECTORY -- so `name/` silently stops ignoring
# the moment this repo's own worktree practice makes that path a symlink onto the primary's copy, which is
# what the documented route for a fresh worktree PRODUCES. Measured on the agent host 2026-09-22: 118 of
# 118 worktrees had a symlinked `node_modules` and none of them was ignored, and one of them had already
# committed it. The slash-less form is strictly WIDER -- it still covers the real directory -- so there is
# nothing to weigh up. `packages/lab/src/packaging/gitignore-covers-symlinks.test.ts` drives every entry
# below through a real `git check-ignore`, as a symlink and as a directory, and fails on a new `name/`.
node_modules
dist
.env
.venv
__pycache__
*.log
.DS_Store
# `runs` WAS THE FIRST INSTANCE of the defect the header states, and the record of it stays here because
# it is the reason the rule is believed rather than assumed: on the lab container `runs` is a SYMLINK onto
# a mounted volume (/srv/a11y-runs) so the corpus survives a container rebuild -- and git's trailing slash
# means "directory", which a symlink is not. So `runs/` matched on a developer's Mac, where it is a real
# directory, and silently did not match on the box that actually holds the corpus: it showed as untracked
# in every `git status` there, one `git add -A` away from committing a symlink pointing at a path that
# exists on exactly one machine. The fix then was a SECOND, anchored line (`/runs`) beside `runs/`, which
# fixed this path and left the same defect in eleven others -- #1983 found it again in `node_modules` and
# `.venv`. The line below now covers both shapes on its own, so the second line is gone.
runs
# Downloaded encoders and scratch artefacts are not ours to version. The TRAINED SCORER is: it is 27 KB,
# it is this project's actual output, and without it in the repo the LLM-free judge cannot run for anyone
# but the machine that trained it — which is exactly how a shipped Action would have had no model at all.
# The training report ships with it as provenance: which corpus, which encoder hash, which thresholds.
# Note the shape: `models/*` and not `models/`. Git does not descend into an EXCLUDED DIRECTORY, so a
# negation under `models/` can never fire and the files could only be added with `git add -f` — which
# means a future retrain would silently not be committed. Each level has to be re-included in turn.
# The weights now live in `@a11ign/scorer` (PLAN.md M3), so only the ENCODER is ignored — the 27 KB of
# heads and the training report are tracked, and nothing under `packages/scorer/models` needs re-including
# level by level because no ancestor is excluded any more. The lesson above still applies to `models/*`
# below, which is kept because a retrain writes its intermediate output there.
packages/scorer/models/encoders

# `npm pack` includes UNTRACKED files, so the encoder sitting inside the package directory would be shipped
# to every consumer if `packages/scorer/package.json` listed `models` instead of `models/screenreader-scorer`.
# That is why the allow-list names the subdirectory.

models/*

# Corpus snapshots (scripts/corpus-snapshot.mjs) — local archives, never committed
backups

# tsc --build output from the project-reference fixtures (src/packaging/project-references.test.ts)
scripts/tsconfig-fixtures/*/dist
scripts/tsconfig-fixtures/*/tsconfig.tsbuildinfo

# Package build output (npm run build)
packages/*/dist
packages/*/tsconfig.tsbuildinfo

# OUR tailnet, not the project's. This repo is generic; the Tailscale policy below is one deployment's
# access rules and one home network's addresses, and neither belongs to anyone else using this tool.
# `tailscale.yml` is kept because it derives everything from `inventory.yml` and so works for any fleet —
# the POLICY is what is specific, not the mechanism.
packages/worker-fleet/tailscale-acl.hujson

# Hosts that are ours and are not capture infrastructure. `tailscale.yml` merges this with the inventory
# when it exists, so a personal machine can be reachable over the tailnet without being published in a
# public repo as though it were part of the fleet.
packages/control/ansible/*.local.yml

# Whatever agent tooling the person at the keyboard happens to use.
#
# `.agents/` and `.clinerules/` are per-operator assistant configuration, and `skills-lock.json` pins
# third-party skills from someone else's account. None of it is this project: a stranger cloning the repo
# would inherit one contributor's editor setup as though it were a build requirement. Same reasoning as
# the Tailscale policy above — the mechanism can be shared, one person's configuration cannot.
#
# Ignored rather than deleted, because they are useful locally and the point is only to keep them out of
# a public, generic repo.
.agents
.clinerules
skills-lock.json

# Coverage output. Regenerated by the test run, never reviewed from the tree.
coverage

# GENERATED, and DELIBERATELY UNTRACKED -- issue #158, ceo-ruled 2026-09-07: "Generated files,
# docs/coverage.md first, are regenerated and checked in CI, never committed in a PR." Committing a
# derived snapshot that many unrelated branches regenerate is a conflict generator in a file neither
# author wrote a line of -- `git log` already showed it moving under commits whose subject was a
# criterion change, not a docs change. `npm run docs:coverage` writes it locally; `coverage-doc.test.ts`
# regenerates fresh and checks the SHAPE (a real count, at least ten rows), which needs no committed
# copy to diff against.
docs/coverage.md

# Written by provisioning ON THE GUEST, into its own checkout. Untracked here on purpose — they describe
# one machine, not the repo. Ignored so they cannot dirty a guest's tree: `deploy.yml` and
# `provision-role.yml` both fast-forward with `git merge --ff-only`, and `run-job.yml` REFUSES to run at a
# commit other than the one asked for when the checkout is dirty. An untracked artefact silently blocking
# a pull is the exact fault that left the lab 17 commits behind for days.
provision-revision.txt
provision-commit.txt

# The real fleet inventory: real addresses, in a public repo. Posture change, ceo-ruled 2026-09-06 (#54).
# `inventory.example.yml` is the committed, syntax-checkable stand-in — same worker names and group
# structure, placeholder addresses — and the real file is restored from the secrets store at bring-up on
# the control plane. `inventory-example-parity.test.ts` keeps the two in shape-sync whenever the real file
# is present locally; it skips honestly when it is not, which is the expected state everywhere else.
packages/control/ansible/inventory.yml

# `.metadata_never_index` IS IGNORED PER-DIRECTORY ON THIS MACOS, MEASURED -- and this line stays anyway.
#
# THE CLAIM THIS COMMENT ORIGINALLY MADE WAS FALSE. It said the marker is "how you say so
# per-directory". Placed on all 68 worktrees at 12:47Z and verified present (53 under Documents, 15
# under /private/tmp), `mds_stores` read 54.8% at 12:45Z and 80% at 12:52Z. No effect. On current macOS
# the marker is honoured at a VOLUME ROOT only; per-directory exclusion is the Spotlight Privacy list,
# which is a machine-owner action and not something this repository can do to somebody's Mac.
#
# The line stays because the marker file must never become a tracked artefact regardless of whether it
# works -- see below -- and because a future macOS may honour it.
#
# Measured 2026-09-09: 119 worktrees on this host, `mds_stores` the top CPU consumer at 70-94% for
# hours with no git running, and `syspolicyd`/`trustd`/`diagnosticd` filling the rest of the top five --
# Spotlight and Gatekeeper walking every checkout, each a full copy of the source tree. The prune took it
# to 67; the marker stops the remainder being walked at all.
#
# IGNORED RATHER THAN TRACKED, deliberately. A tracked `.metadata_never_index` would appear in every
# worktree automatically, which sounds better and is worse: it would also sit in the PRIMARY checkout and
# in every consumer's clone, making a decision about somebody else's machine that is not ours to make.
# Ignored means `scripts/spotlight-exclude.mjs` places them locally and nobody's `git status` shows one --
# which is the whole reason this line comes first: without it, 67 untracked files appear across nine
# sessions' working directories, and `git add -A` sweeping another agent's files is the incident the
# pre-commit hook exists to catch.
.metadata_never_index

# The worktree owner stamp (#1128) -- metadata about WHOSE tree this is, never work.
# Untracked-but-visible would make every stamped tree read dirty to `git status --porcelain`, which is
# exactly what `prune-worktrees.mjs`'s isWorkingTreeClean asks before reporting a tree safe to delete.
.a11y-owner
