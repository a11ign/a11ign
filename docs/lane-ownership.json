{
  "_owner": "ceo",
  "_why": "ceo's ruling 2026-09-08: 'a workflow change outside its owner's lane reached main through a pipeline you own without you refusing it.' A lane is who may CHANGE a path, which is a different question from owned-path-facts.json's (what a change to a path must DECLARE) -- so it is a separate file with a separate owner. Data, so ceo can move a lane without touching packages/agent-org/src/workflow-lane-check.mjs, and so the mechanism cannot quietly decide who owns what.",
  "_exception": "A lane is not a wall. ceo assigns across lanes deliberately -- #548 was ceo's assignment to the PM, in ceo's words, and the PM asked first. The exception is a line in the PR body, and it is PRINTED by the check rather than merely accepted, so a deliberate crossing is in the log rather than in somebody's memory. See the Lane-exception format in the script's header. AND THE BOUNDARY ITSELF MUST BE RIGHT FIRST: a generated file belongs to its generator's owner, and an exception is for a deliberate crossing of a CORRECT boundary, never for a wrong one. ceo, 2026-09-09, after `.github/workflows/` as a flat prefix would have refused #558 -- whose author flagged it before pushing rather than routing around it. Asking them to write a Lane-exception for a mis-stated rule would have been the guard-people-route-around failure this exception exists to prevent, arriving one door along.",
  "_claimVsAuthorRuling": "ceo's ruling 2026-09-18, from the chairman's own observation: 3 engineers idle all morning behind a 4-row `lane:ceo` queue clearing at ~1 row/tick, because `laneReason` (packages/agent-org/src/row-claim/runner-rule.mjs) refuses ANY session that is not the row's lane owner, unconditionally -- a PR-body `Lane-exception:` line and even a ceo comment saying 'assigned to worker-tooling' change nothing at CLAIM time, only the label does. ceo had been trying to personally author all 4 rows and losing turns to B4 file-overlap refusals against its OWN other .github/workflows/ PRs, which is what actually stalled the queue, not row content. THE RULING: this lane's own `why` above is a REVIEW concern (trunk health, a red check nobody owns) and its own `_exception` already contemplates an engineer building under a Lane-exception line -- so `lane:ceo` on a row protects ceo's REVIEW, not ceo's AUTHORSHIP, unless the row itself asks for a decision only ceo can make (the publish order, a freeze, a ruling -- e.g. choosing between two release-dispatch behaviours, #1452). Applied same day: #1320, #1257, #1397 (mechanical fixes needing review, not judgment) re-laned `lane:ceo` -> `lane:any`; #1452 (a genuine choice about release-dispatch semantics) kept `lane:ceo`. Going forward: before leaving a row `lane:ceo`, name which of the two it is on the row -- a path needing review re-lanes to `lane:any` with a Lane-exception line; a decision stays and its wait is a real cost, not an accident.",
  "lanes": [
    {
      "lane": "the pipeline",
      "owner": "ceo",
      "branchPrefixes": [
        "ceo/",
        "revert/"
      ],
      "paths": [
        ".github/workflows/"
      ],
      "why": "trunk health and the merge queue. Owner moved from dispatcher to ceo on 2026-09-11 (#913): dispatcher was retired in the Org Reset of 2026-09-10, and a lane whose owner no longer exists refuses every PR with a message nobody can act on. The check STAYS until #916 retires it with CODEOWNERS on 2026-09-15 (ceo's second ruling of 2026-09-11 on #902, reversing the first: an unassigned crossing must be refused, not merge silently); an engineer's pipeline change is assigned per PR by a Lane-exception line naming ceo, and this sentence is echoed into the refusal, so it must say what is current. trunk health and the merge queue. A workflow edited by a session that is not watching the queue is a red check nobody owns -- measured 2026-09-08, when a pull_request trigger attached a failing audit to seven merged PRs and sat red for ninety minutes before anyone in the org read the view the chairman reads. revert/ is here because trunk-guard's retired revert job used to open those branches and they carry whatever the reverted commit touched (#2356: nothing opens one now, and the path stays so a hand-made one is still this lane's).",
      "except": [
        ".github/workflows/consumer-gate.yml"
      ],
      "exceptWhy": "A GENERATED file whose SOURCE lives outside the lane is not the lane's to own. consumer-gate.yml is regenerated from README.md by scripts/generate-consumer-gate.mjs, so its content is contract work that happens to land in this directory -- the pipeline does not decide what it says, and the cost of changing it is visible from the generator's seat rather than from the merge queue, which is the whole test for whether a path belongs to a lane. Added 2026-09-09 after the flat prefix would have refused #558 and forced its author to write a Lane-exception for a rule that was mis-stated: making somebody paper over a wrong rule with a body line is the guard-people-route-around failure the exception was designed to prevent, arriving one door along."
    }
  ]
}
