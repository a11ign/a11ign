# npm Scripts

Every `npm run <name>` script not already covered elsewhere in CLAUDE.md's Mac/fleet cheat-sheet, moved
from CLAUDE.md during #458. **Disjoint** from `docs/commands.md`, which documents `scripts/*.mjs` files
with no `package.json` entry — a different population of commands.

## Every other command, and when you would reach for it

The sections above document a command where the problem it solves is described, which is more useful than
an index. These are the rest — real commands with no natural home above, listed so that none is
discoverable only by reading its own source.

`commands-documented.test.ts` enforces the coverage: every npm script must appear somewhere a human
looks, or be declared INTERNAL with a reason. It was added because three commands and two flags landed in
one day and only one reached this file. **A command nobody can find is a command nobody runs** — the same
failure as `capture-check` being mandatory and never running once.

| command | when |
|---|---|
| `test:nightly` | **the nightly-only test population** (#1135): `packages/*/nightly/**/*.test.ts`, run once a night by `nightly.yml`'s `nightly-only` job and never on the PR path, whose glob (`packages/*/src/**/*.test.ts`) cannot reach a directory beside `src/`. This is where a converted guard's run-property residual goes under #908: the vacuity check that must fail loudly ("discovered 0 where the census is 6") and the exemption table. Same floor-checked runner as `test:ts`, `--min=1`, so an emptied directory is a refusal, never a clean pass. Run it locally before moving a file there |
| `rescue:hunk` | **Rescuing one file's change off a stranded branch (#705).** `git checkout <branch> -- <file>` takes the branch's WHOLE FILE, so every edit main has made since the branch diverged is reverted and the diff reads as the rescue's own insertions — and nothing catches it: `resolve-toward-main` fires on a MERGE whose resolution kept the branch's side, and a rescue never merges. This does the three-way merge instead (`git merge-file`, whose exit status IS the conflict count), prints what main has GAINED in that file since the base whether or not anything conflicts, and REFUSES any hunk that would replace a line main changed — `A11Y_RESCUE_REASON="<why>"` overrides and is printed. **The reverts a rescue makes silently are the ones that change no behaviour**: on the real pair it exists for, a wholesale take un-redacted the lab's IP address in a comment, where no build breaks and no test fails; the same take also reverted four `@a11ign/*` imports, and THAT one `tsc` would have caught. Report-only by default; `--apply` writes the file |
| `worktrees:prune` | **REPORTS by default; `--apply` removes.** After a merge, or whenever `git worktree list` looks long. It removes a linked worktree only when its branch is fully merged into `origin/main` AND its working tree is clean, names every other one as DIRTY with its branch and touches nothing about it, and never the primary checkout (identified by `.git` being a real directory there, a file everywhere else). A rule maintained by hand ("prune after every merge") reached 36 worktrees and 4.4 GB the day after a 28-tree hand-prune. **The default flipped on 2026-09-09**: a session ran it to READ the breakdown before writing a row about worktree accounting, and it removed three worktrees belonging to three other sessions. No work was lost — it refuses anything dirty or unmerged — but **the tool's safety and the hazard are about different things**: it guarantees the BRANCH is merged and the TREE is clean, while the hazard is about the SESSION, whether anyone is standing in that directory. A merged, clean worktree can still be somebody's current working directory, and their next `cd` then runs in the PRIMARY checkout — the fleet-driving tree `assertFleetRunsThisCheckout` hashes. `lab:reset` has the same shape (`-e apply=true`) for the same reason |
| `primary:update` | **the only way to move the primary checkout** — fetch, then detach at `origin/main`, nothing else. Built for issue #126, after three real incidents in one night where the primary ended up holding a branch by accident (a worktree left parked there, `lab:collect-promotion` writing artefacts in with nothing marking the boundary between producing and committing, and a failed `cd` into a deleted merge worktree silently falling back here). Refuses outside the primary — running it in a worktree would detach that worktree from whatever branch it holds, which defeats the point of a worktree. See the `pre-commit`/`post-checkout` entries below for the hooks that make the primary refuse to hold anything else in the first place |
| `fleet:normalise` | bring every LOCAL UTM guest to one baseline, elevated, and prove it took. The bare-metal equivalent is `fleet:provision` |
| `fleet:recover` | **a worker that is UP, ANSWERING and not working.** Measured 2026-09-02 on a11y-worker-6: a capture began at 03:00 and was still `current` at 06:32, with every readiness check green and `busy: true` for three and a half hours — from the run's side that is a slow page, so it waited and a corpus recapture made no progress at all. `fleet:deploy` cannot fix it: `Stop-ScheduledTask` will not end a node process wedged in a capture, the restart loses the race for port 8765, and the old process keeps serving a `/health.code` read from files the deploy just updated — so `verify-code.yml` sees a MATCH and its reboot never fires. This kills node outright and reboots, and PROVES it by requiring `vitals.uptimeMinutes` to have fallen: a worker that answers is not a worker that restarted, and that box answered perfectly for six days. `--limit=<one worker>` (or `target=<worker>`) is now REQUIRED (#1829): this playbook is exempt from the capturing-worker guard on purpose, so an omitted or fleet-wide limit used to be able to reboot a box a DIFFERENT session is mid-capture on right now |
| `fleet:os-rollback` | **one worker whose Windows build has drifted from the fleet's** (#921). The OS build is a capture-cache key, so a feature update that slips past the appliance policy leaves the fleet on two builds and every corpus-wide job routing around the odd box. PXE plus `autounattend.xml` is a BUILD path, not a rebuild path: a box with a working install boots it, so `fleet:recover` reboots without rebuilding. This rolls the box back with `DISM /Online /Initiate-OSUninstall` while Windows still keeps the previous build. `--limit=<one worker>` is REQUIRED, and without `--apply` it only reads (the rollback window, `Windows.old`, the firmware's network-boot entries) and says which path is open. It refuses a box already on the build every other worker agrees on, and a box mid-capture. The proof is the build `/health` reports afterwards, never DISM's exit code |
| `guest:run` | run a script on a UTM guest elevated and actually get its output — `utmctl exec` returns exit 0 and no output whether or not it ran |
| `fleet:inventory-install` | **put `inventory.yml` where a `git pull` cannot delete it.** The file is untracked and gitignored (#54), so a pull removes it from any checkout holding one — and on 2026-09-06 the CONTROL PLANE pulled main, lost it, and `fleet:deploy` printed `skipping: no hosts matched` and **exited 0** with ten workers untouched. This installs it at `/etc/a11ign/inventory.yml`, which `ansible.cfg` now reads FIRST, and verifies arrival by SHA-256 read back over a channel that shares no failure mode with the write. A playbook rather than an `scp` for this file's own reason: an operation that matters is CLI-invocable and reviewable, or it is done differently every time by whoever is at the keyboard. Additive and idempotent — it never touches the in-tree copy, which stays as the migration fallback |
| `fleet:control-host-install` | **the sibling of `fleet:inventory-install`, for `A11Y_CONTROL_HOST` itself** (#285). Ten bare-metal workers sat idle because the shell dispatching `fleet:deploy` had never had the variable set, and `requireControlPlaneHost()` correctly refuses rather than guessing (#83). This installs the value it just used at `/etc/a11ign/control-host` on the control plane — the third state between "committed to git" (never) and "typed into every shell" — so a future shell that never set the variable falls back to the file instead of refusing. `requireControlPlaneHost()` checks the env var first, then the file, then still refuses when neither exists; the installer never reads the environment itself, it records what `fleet-playbook.mjs` already resolved to reach the machine it is writing to. `A11Y_PVE_KEY` deliberately gets no equivalent here — see #285 for why it stayed its own decision |
| `fleet:tailscale` | put the fleet on Tailscale |
| `fleet:link-view` | **are the workers ABSENT at layer 2, or present and silent above it** — the question that decides whether a person is sent to the power strip or the switch, and the one no other command could answer during the 2026-09-12 outage when all ten boxes stopped answering HTTP, SSH and ICMP at once. `collect-logs.yml` reads the workers, which is exactly what you cannot do when they are gone; `fleet:status` says `unreachable` and stops. This asks three vantage points — the control plane, the lab and the hypervisor, three separate network stacks on one subnet — for their neighbour table, filtered to the addresses `inventory.yml` already defines. Read-only, `changed_when: false` throughout: during an outage an action taken before the link question is answered destroys the evidence that would have answered it. It DOES send one ICMP echo per address, because a `STALE` neighbour entry is a CACHED answer from before the incident and reporting it as presence is the likeliest way to send somebody to the wrong thing — the probe forces re-resolution, and the read then retries until no address is left in `PROBE`/`DELAY`/`INCOMPLETE`, which are not answers but "still asking". Also reads `journalctl` on the box holding the fleet key, so **"nothing ran against a worker in that window" is a finding it can state** |
| `layers:compare` | **the project's central claim, demonstrated**: which findings can ONLY the screen-reader layer produce? Asserted for a long time before anyone ran the two side by side |
| `fleet:hours` | **what did a capture run COST the fleet, in worker-hours** — the method is EMITTED by the tool (`--json` carries a `method` field) so a board record copies it rather than retyping it — one fact, one place. Measured 54.11 worker-hours across the 5,395 captures on disk, median 27.4s, p95 134.2s. It REFUSES rather than printing 0.00 when it billed nothing, and tells "no JSON found" apart from "walked N and billed none" because those need opposite fixes. Built after the obvious method — sum the per-case times in `capture-progress.json` — turned out to describe data that does not exist: **0 of 1,623 cases carry any time field**, and only the transient `current` array holds `startedAt`, which an entry leaves the moment the case completes |
| `verdict:stability` | is an OCCURRENCE verdict stable on a flaky substrate? |
| `eval:capture` | recapture the eval fixtures, over a live worker or in-process on the guest |
| `capture:explain` | **what actually happened on a page, in sentences, from marks the capture already recorded** — did it reach the page, did the screen reader reach the CONTENT, was a consent banner in the way, did the page move under its own probes. A capture carries ~30 diagnostic marks and NOTHING read them: every question was answered by ssh, hand-written Python and a guess at the JSON shape, which produced four wrong answers in one session — the worst being the wrapper read instead of `capture`, reporting 0 of 20 tab stops. Absent prints as NOT RECORDED, never as OK |
| `rules:score` | `rules:gate` without the gate — the per-criterion detail rather than a pass/fail |
| `scorer:verify` | **is the SHIPPED model directory free of unsafe artefact types?** `.pt`, `.pkl`, `.ckpt` and friends are executable-on-load weight formats; safetensors is not. The script existed and NOTHING invoked it — not an npm script, not a playbook, not another module — so a security check on the one artefact this project publishes had never run. First stage of `release:gate` now |
| `release:provenance` | **do the weights about to ship have a changelog entry saying where they came from?** ADR 0007 makes the weights the API and `promote:model` writes their provenance into a changeset, which is the only record of it — and nothing checked that the entry described the weights actually present. Found the shipped model at 2,485 records while both pending changesets said 2,403 and were BYTE-IDENTICAL to each other, so a first release would have published weights nothing accounts for beside one note printed twice. Both are invisible elsewhere: a changeset is prose, and `changeset-provenance.test.ts` asserts how a row RENDERS, never that it describes what ships. Second stage of `release:gate` |
| `scorer:migration` | is a schema migration open? `release:gate` runs it first and refuses while one is. `lab:inventory` also reports it, with which candidate could close it |
| `release:rehearsal-check` | **is the commit about to be released the one the most recent V1 rehearsal (#813, #324) actually ran against?** `RELEASE.md`'s own `<!-- REHEARSAL:COMMIT <sha> -->` marker names it; this REFUSES, printing both shas, when the release commit is not that one — a rehearsal covers the one commit it ran against, never the tip of a branch by assumption. Third stage of `release:gate`/`release:gate:ci`, right after `release:provenance` |
| `scorer:retired-heads` | **did the candidate's head set SHRINK, and is every disappearance declared?** `3.3.2:unnamed-form-field` almost shipped absent with nothing accounting for it — a legitimate removal, fully reasoned in `case-matrix.mjs`, that still cost an evening because nothing made the reasoning travel with the change. A head present in the shipped report and absent from the candidate is a REFUSAL, never a warning, unless a `retired-heads.json` beside the shipped weights names the subtype, when, why, and where the reasoning lives — created the first time a head is actually retired, same as `schema-migration.json`. Second stage of `candidate:gate`, right after `scorer:migration` — deliberately not in `release:gate`, since the candidate is the only place the question can still change the outcome |
| `candidate:gate` | the gate chain against a CANDIDATE rather than the shipped weights — the question `release:gate` structurally cannot ask |
| `promote:model` | copy trained weights into `packages/scorer/models/` and write the changeset. **Refuses when a previous promotion is still uncommitted where this one would write** — this line claimed that for months and the script did not import `node:child_process` at all, so it could never look at git. It checks the TARGET paths rather than the whole tree, because this is a shared checkout and a guard that fires on somebody else's unrelated edit is one people bypass |
| `promote:gated` | `candidate:gate` and then `promote:model`, which is what `lab:job -e job=promote` runs. Never promotes on a failed gate |
| `training:capture:fresh` | generate the pages and then capture them. Capturing without regenerating is testing the previous commit |
| `training:export:test` | export accepting captures whose PAGE HAS MOVED, so a change can be tested without a full recapture. Every record is stamped `grade: "test"`, the trainer refuses to mark such a model release-eligible, and `promote:model` refuses an ineligible one — three gates, so a test dataset can never ship. Use it with `--pipeline=verify`; never for a release |
| `training:build-realism` | add the real-page tier to the exported dataset |
| `training:check-signals:complete` | `check-signals` that REFUSES a partial corpus, rather than scoring what happens to be on disk |
| `training:capture-acceptance`, `training:export-acceptance`, `training:export-acceptance:all` | the held-out set. Never cached, because those runs exist to test whether NVDA's output is still stable. The `:all` one exports EVERY repeat, because the evaluator reads every repeat — two jobs feeding one consumer is a drift generator, and produced a held-out score computed half on each |
| `training:evaluate-acceptance:shipped` | score the SHIPPED model from a COPY under `runs/`. `release:gate` calls this one: the plain `training:evaluate-acceptance` defaults `--model` and `--training-report` into `packages/`, and the evaluator refuses to stamp a verdict into tracked source — *"that record describes what was true when those weights shipped"* — so the gate could never pass as written |
| `training:train-baseline` | train without the realism tier, for comparison |
| `corpus:distribution` | **is any field empty on EVERY record?** No page can cause that, so the probe or exporter filling it has stopped — and `postSubmitFields` was `[]` on all 2,122 captures with every check green, because *an empty field is not a malformed one* and no count ever moved. Empty on SOME records is often the finding and is deliberately not reported. Also catches a collapsed label balance and a missing required key. First data-shape gate this repo has had; runs inside `release:gate` |
| `corpus:grants-audit` | **does a multi-defect page carry the evidence its labels claim?** Every accompanying defect declares a `grants` feature and nothing read it — so a label for a defect whose evidence was never captured passed every gate. Needs the AUTHORITATIVE corpus: `lab:job -e job=grants-audit` |
| `release:gate:ci` | **the five `release:gate` stages a GitHub runner can actually prove** — the artefact is safetensors, the shipped weights have a changelog entry accounting for them, no schema migration is open, and every package is usable installed from a tarball. It exists because `release.yml`'s first ever dry run died `sh: 1: .venv/bin/python: not found` at stage 4 of 12: eight of the chain's thirteen stages need the Python venv or the CORPUS, and the corpus is gitignored and hours of fleet time, so `release:gate` in CI could only ever fail — and a gate that cannot pass is one people stop dispatching. **It is not a substitute**: run the full gate on the lab with `npm run lab:job -- -e job=release-gate` before publishing for real, which is what the human typing `publish-for-real` is asserting |
| `scorer:shortcuts:baseline` | **RECORD** the veto baseline against the shipped weights — `scorer:shortcuts` without the gate and with `--update-baseline`. Writing it asserts that every veto it holds is understood and accepted, so it is a deliberate act and never a side effect of auditing. It exists because the `shortcuts-baseline` job called the Python **directly** and so was the one job that never ran `corpus:unclosable-map` first: the audit refuses an ABSENT map and cannot see a STALE one, so the tracked baseline could be written against a classification several commits old, silently. `lab-job.test.ts` now refuses any job that reaches a derived-input script without its chain |
| `scorer:shortcuts:baseline:candidate` | the same deliberate act against the CANDIDATE, and **without it a new head can never be promoted**. `candidate:gate` blocks any head missing from the baseline — by design — while `scorer:shortcuts:baseline` reads the SHIPPED weights, which cannot contain a head that has never shipped. The two requirements were circular, and adding 3.3.3 on 2026-09-02 was the first thing in a long time to walk into it. Separate from its sibling rather than a flag on it, because which model's vetoes you are accepting is not something a reader should have to infer |
| `scorer:explain` | **Why did the scorer decide that?** `--compare a,b` (two models, criterion by criterion), `--model=m --criterion=X` (which cases a criterion got wrong, with the cut, and — for a criterion that failed — the pasteable `--case=` list of all of them), and **`--model=m --case=<id>[,<id>…]`**: for a held-out acceptance case, its label, each head's score against its own cut, and the value of EVERY document feature by name — read from the records the model's own acceptance report names, through the featurizer the evaluator scores with, never a second implementation. It exists because `explain-feature` reads the TRAINING export and could not say whether a vetoed feature was 0 on a MISSED acceptance case (#2258, #2334). An id no record holds is refused by name. Read-only. `lab:job -e job=explain-case -e case=… [-e out=…]` |
| `scorer:explain-feature` | **WHY does a feature read what it reads on a subtype's positives?** `scorer:shortcuts` says a head vetoed one, `corpus:starvation` says one will be constant — neither says why, and the three possible answers need OPPOSITE work: definitional (`IMPOSSIBLE_BY_DEFINITION`), a probe that never ran (a capture change), or a thin corpus (the only one ADR 0015's remedy fits). It computes through the real featurizer rather than restating it, and prints the CHANNELS for records that read 0 — because *a count is where an investigation stops*, and the interesting part is usually a field the feature does not read. `lab:job -e job=explain-feature -e subtype=… -e feature=…` |
| `corpus:observation-ambiguity` | **how many feature zeros are capture ARTEFACTS rather than page facts?** Every structured feature is `float(bool(channel))` and `any([])` is `False`, so a `0` means both *the page has none* and *nothing looked* — and the model is trained on the two mixed together. `landmark_present` was DELETED because **16 of its 16 zeros were truncated sweeps**; `heading_present` and every other channel-presence feature carry the same exposure and none had ever been measured. Reports, never blocks — it describes the capture path, not a defect a commit introduced. Needs the AUTHORITATIVE captures: `lab:job -e job=observation-ambiguity`, because a laptop's copy answers `unknown` everywhere |
| `corpus:container-exits` | **does NVDA ever announce leaving a landmark?** The fact `vague_link_lacks_context` rests on: a container that never closes stays open for the rest of the transcript, so a rule reading it as CONTEXT is describing the top of the page rather than the announcement in front of it. Reports, never blocks — it describes NVDA, not a defect |
| `corpus:unclosable-map` | emits the two tables of vetoes NOTHING CAN CLOSE, for the Python weights-side audit to read. `audit-corpus-starvation.mjs` has carried `IMPOSSIBLE_BY_DEFINITION` for months — *"reporting those put items on a work list that nobody can complete"* — and `audit-scorer-shortcuts.py` never learned it, so `scorer:shortcuts` reported 57 veto pairs with no way to say which were worth corpus work. Run by `scorer:shortcuts`; separate because `test_unclosable_map_is_current.py` pins the two sides equal, and it caught a stale entry naming a feature the pipeline has never computed on its first run |
| `corpus:grants-map` | emits the JS-side `grants` declarations for the Python audit to read. Run by `corpus:grants-audit`; separate because the audit REFUSES without it rather than examining an empty set |
| `scorer:shortcuts:candidate`, `training:evaluate-acceptance:candidate`, `corpus:applicability-audit` | the three stages that existed ONLY as Ansible job argv, so the npm chain could not run them and silently did not. `evaluate-acceptance:candidate` is the one that matters: it writes the report `promote:model` requires, and without it a whole run ends at *"model-candidate is not releasable: held-out acceptance has not been run against these weights"* with every earlier gate green |
| `changeset:status` / `release:version` | changesets, as usual |
| `mutate` | **mutation checking as a COMMAND, because five steps typed by hand is five steps that get typed wrong.** `npm run mutate -- --file=<path> --mutate='<shell>' --test='<shell>'`. It runs the test FIRST and refuses if it is already red; copies the file aside rather than `git checkout --`, which restores to HEAD and silently discards every uncommitted change in it; **proves the mutation actually landed**, because a shell-quoting slip makes the edit a no-op and the resulting pass reads as *the guard does not bite* when it means *nothing was broken*; requires the test to FAIL; then restores and **runs the test again**, which is what proves the restore worked rather than that `cp` exited zero. Exit codes are the contract: **0** the guard bites, **1** it did not — suspect the guard before the code, **2** refused before mutating, **3** the restore failed and the copy is left in place. Built 2026-09-06 after two agents got the sequence wrong in one day: one destroyed uncommitted work with `git checkout --` mid-check, and two guards shipped GREEN against the very defect they were written for |
| `board:report` | **the daily board report, generated from GITHUB AND GIT rather than from what an agent said.** Issues and the milestone come from the API, merges from `git log main --merges` with the window stated — because two correct counts over different windows read as a disagreement, measured on its first edition where a peer's 17 (since 09:00) and the script's 42 (since midnight) were both right. The two figures neither source can supply — the last gate result and the fleet-hours total — are quoted from `docs/board/reported/`, where the agent that RAN the command records its verbatim output, who ran it and when; absent or stale prints as *not reported*, never omitted and never estimated. **It deliberately does not read a gate itself**: a checkout's `runs/` is only as fresh as its last sync, and one measured here was 89 hours old and answered cleanly having examined a corpus that no longer existed. `--post --issue=<n>` publishes an edition as a comment; generating and posting are separate acts so a bad report can be seen before it is posted |
| `tracker:comment` | **the one place that reads or edits a tracker comment**, so no edit is improvised per call site (#563). Correcting one wrong issue number inside a comment on #546, a hand-rolled read used `issues/{issue}/comments/{id}` — which is not an endpoint — captured the 404's JSON out of stdout, `sed`ed it, and PATCHed it over the original; the comment's entire body became the error document. **`gh api` prints a well-formed, parseable error to STDOUT and exits non-zero**, so a command substitution turns a failure into a plausible value: valid JSON, non-empty, and nothing downstream can tell it from content. This checks the exit status AND the shape — the status is the reliable signal, the shape is what survives a caller who dropped it — and refuses a write whose read did not succeed. `edit` is **compare-and-swap**: it names the digest `read` printed, and refuses if the comment moved in between, because two sessions edit this tracker and overwriting somebody's correction is the same failure with a politer cause |
| `board:record` | **the WRITE path for an achievement record, and it refuses before the file exists** (#577). On 2026-09-08 the body sat at exactly its 925-word cap, two records were added, and their bullets were precisely the overage: `board-style.test.ts` refused, `trunk-guard` went red, and main stayed red until the revert. The next achievement anyone recorded was going to do that, to whoever recorded it. `bodyCapRefusal` was already thorough and ran in the wrong place — inside `board:document`, at 08:00, hours after the merge, and **a refusal that arrives after the merge cannot stop the merge**. So this runs the same refusal (imported, not restated) against the board as it would be WITH the draft, and writes the file only if it still fits. It **names** the candidates for retirement oldest first with their word costs and **retires nothing itself** — which claim has stopped earning its place is a person's judgement, and making it automatically would make it in the same hurry the refusal exists to prevent. Nothing is written when it refuses: "it refused" and "it wrote the record then complained" are indistinguishable from an exit code, and the second leaves the trunk exactly as red. **It also assigns the record's `order`** — the next free value, spaced by tens — and refuses a draft naming one already held, or a second record for an issue that has one, naming the holder and the free value. A writer has no reason to know which values are taken, and requiring them to is how #324's record landed on 40 beside #311's and left the tree failing a guard this tool exists to satisfy early (#806) |
| `board:document` | **what the BOARD reads, rendered to PDF** — the GitHub edition is the data trail, this is the answer. Same data layer (`board-data.mjs`, extracted rather than copied, and proved output-identical to the pre-split generator), same refusal, and it matters more here because a PDF that reaches a board is harder to retract than a comment. Markdown -> HTML is `board-markdown.mjs`, ours on purpose: pandoc is not installed and a dependency the board's daily document cannot be produced without is a worse risk than a plainer typeface. PDF via headless Chrome, already present because the fleet needs a Chromium. **Section 2 refuses to invent a date**: V1 is defined nowhere, so it says so and proposes a definition rather than estimating, and the track status prints the rule it was derived from — a status word whose derivation is not on the page is an opinion wearing a measurement's clothes |
| `ready:audit` | **does any open issue carry `ready` alongside a label that already means "not pickable"** — `fleet-gated`, `disputed`, `decision`, `awaiting-merge`, `blocked`, `review-only`? Filed 2026-09-06 after `dispatcher` labelled two rows `ready` to hit a floor while one was disputed and the other had no Region or Acceptance — "a floor met by a label I control is not a measurement". `review-only` is for a row filed to solicit review or a decision and never meant to be started as work (#27's shape), which had no label of its own until then |
| `guards:sweep` | **every TREE-WIDE GUARD, run together — #716, prerequisite for #704.** `node packages/guards/src/tree-wide-guards.mjs \| xargs npx tsx --test`, behind an npm script rather than typed as prose specifically so `commands-documented.test.ts` can see it (a script name in `package.json`; a raw pipeline in an issue body cannot be found the same way, and #716's own PR was refused on push for exactly that shape mismatch before this script existed). A tree-wide guard's population is the whole tracked tree, so its own green run on a PR's diff is not a prediction the way an ordinary test's is; #716 made the five costliest ~5x cheaper (652 per-path `git ls-files` spawns collapsed to one `Set`; a 3-repo fixture built once and reused, reset between tests rather than rebuilt) and reported the rest as measured rather than forced down where a real `npm pack` invocation is inherent to what a test proves. Discovery is import-based, never a grep: a guard is in the population because it imports `packages/guards/src/tree-wide-guard.mjs`'s marker, the same shape B8 (#621) uses to derive a test's requirements from its import closure rather than from scanning source text |
| `select-changed-tests` | **A1b: which TEST FILES does a diff actually reach, by transitive import, so the PR `ts` job stops running the same suite `trunk-guard` already runs on every merge.** `ci-changed.mjs`'s existing `testPackages` (the transitive closure of dependent PACKAGES) is the search scope and safety net, unchanged; this narrows WITHIN it to files, walking every candidate test's own import closure (relative imports resolved `.js`->`.ts`, a bare `@a11ign/*` specifier resolved through `exports` back to SOURCE rather than `dist/`) and selecting a changed source file's reaching tests. **A changed file no test's closure reaches falls back to its own package's full suite** — the load-bearing half, so a zero can never be silent. Any change OUTSIDE `packages/*/src/` (a root config, `scripts/*.mjs`) steps aside for `ci-changed.mjs`'s own coarser "every package touched" rule rather than being re-narrowed here, since a config file is imported by no test and would otherwise read as maximally uncovered |
| `board:summary-check` | **is TODAY's hand-written board summary written?** Two runs on the MORNING of the edition — 07:15 London reports the absence and exits 0, 07:45 reports it and FAILS — so the warning arrives 45 and 15 minutes before the 08:00 edition. **This entry said "runs at 21:00 as a second launchd entry … eleven hours before" until 2026-09-09, and both halves of that were stale**: the launchd path was retired for GitHub Actions on 2026-09-06, and the board moved the check to the morning on 2026-09-08 (`2a1bdd92`) in its own words — *"it should be 30 mins before as it should be as fresh as possible as a lot happens over night"* — because a summary written the evening before is a forecast about a night that has not happened. The evening run is retired ON PURPOSE; restoring it means taking that back to the board. The stale sentence survived because the `// command:` header that GENERATES `docs/commands.md` still said 21:00 too, so one wrong line was being reproduced into a second document on every docs run. **It generates no summary text**, which is the whole point: the moment it wrote one it would BE the machine-written summary the gate exists to prevent, arriving through the warning instead of through the document. One comment per date, not per run, because a warning that repeats is a warning people filter. `--post` is what the scheduled entry passes; without it the command only reports. **It asks the same question of `docs/board/reported/`, which carries more** (#131): the summary is one paragraph, that file holds every number the document quotes that no gate can recompute — the gate outputs, the fleet-hours figure, the capacity note, every achievement — and both are read by the 08:00 job from `origin/main`, so both have the identical failure of a complete record sitting on the wrong side of a merge. It happened three times on 2026-09-06, each caught by a person typing `git show origin/main:...` by hand and none by a tool. It **names the entries that differ** rather than reporting that the file changed, keyed on each section's own identity field so inserting one gate does not re-label every gate after it, and it **reports rather than refuses** — editing that file and not pushing yet is a normal working state for hours; what must never happen is believing it is published |
| `pr:open` / `pr:edit` | **check a PR body's Acceptance/Closes with the tree's OWN parser (`packages/agent-org/src/acceptance-commands.mjs`) BEFORE `gh pr create`/`gh pr edit` ever sends it** — #746, filed after four PRs went red on the body in one day, four authors, four different modes (a duplicate `Acceptance:` section, prose under it, a pipe the file pre-check cannot parse, the section under `## Verified` instead of `## Acceptance`), none of them a defect in the change. `packages/agent-org/src/pr-open.mjs` wraps `gh pr create`/`gh pr edit`, reading `--body`/`--body-file` the same way `gh` itself does and refusing with the parser's own message — no second parser, calling the exact functions the "acceptance / run" CI job calls. **It cannot help a body edited in the web UI**, where a `## Verified` heading is most likely to be typed; that path stays unguarded, the mirror of #735's row-template gap. Requires `--body`/`--body-file` explicitly — an interactive `gh` editor session is not checkable before it is sent |

