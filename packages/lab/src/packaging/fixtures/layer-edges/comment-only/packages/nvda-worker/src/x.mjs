// import { X } from "../../other/src/x.mjs";  -- the old reach, kept in a comment as history
/*
 * readFileSync(new URL("../../other/src/x.mjs", import.meta.url), "utf8")
 * join(ROOT, "packages", "other", "src", "x.mjs")
 */
export const NOTHING = 0;
