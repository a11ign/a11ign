---
# Run a long lab job by NAME, and wait for its real exit code.
#
#     ansible-playbook lab-job.yml -e job=train
#     ansible-playbook lab-job.yml -e job=sweep -e model=runs/model-candidate
#     ansible-playbook lab-job.yml -e job=capture-real-pages -e role=training -e shard=0/4
#     ansible-playbook lab-job.yml -e job=evidence-check -e only=media-autoplay-audio -e worker=a11y-worker-2
#
# The `capture-real-pages` line carried `-e worker=a11y-worker-2` until #1956, and that job has REFUSED
# `-e worker=` since it moved to the whole fleet — the file's first example was a command the file itself
# rejects. `evidence-check` is where a named worker belongs now (#1948): optional there, and it aims a
# narrowed read at one box rather than spreading it across the pool.
#
# ## Named jobs, never a command
#
# The catalogue below is the whole interface, and that is the security design rather than a convenience.
# This plays against the box holding the capture corpus (not reproducible — `browserVersion` is a capture
# cache key), the `a11y-corpus` GitHub deploy key, the release weights, and the ability to wake twelve
# Windows machines. `ansible/README.md` rejected an `/admin/update` route on a WORKER because "a mutating
# route there is unauthenticated remote code execution on twelve boxes"; a route that runs training jobs is
# strictly worse, since executing code is its entire purpose. Ansible is agentless, so there is no route to
# secure — but a free-form `command` parameter would put one back.
#
# So: no `command` variable exists. `argv` comes from this file, and the only things a caller supplies are
# CHOICES, checked below.
#
# ## The parameters are enums and names, not paths and URLs
#
# `worker=a11y-worker-2` resolves through the inventory to an address. That is not decoration: it makes an
# arbitrary URL INEXPRESSIBLE rather than rejected, which is the same shape as `isValidCaptureId` on the
# worker ("path traversal is the reason this check exists"). `role` and `shard` are checked the same way,
# and note `--shard` is validated a second time by `shard.mjs` at the far end — deliberately, because a
# malformed shard silently doing the whole list means four runs each fetching all 77 pages from other
# people's sites.
#
# ## Deliberately NOT here
#
# No env passthrough. `A11Y_PYTHON` is read at four sites in this repo and becomes the executed
# interpreter, so a caller-supplied environment is arbitrary code execution with a whitelisted job name and
# an empty argv. `run-job.yml` builds a fixed env and nothing reaches it from here.
# THE LAB MUST BE IN THE INVENTORY, AND AN EMPTY INVENTORY MUST NOT READ AS SUCCESS.
#
# `ansible-playbook` prints `skipping: no hosts matched`, an EMPTY PLAY RECAP, and **exits 0**. So every
# command in this directory could fetch nothing, stop nothing, reset nothing and report success — measured
# 2026-09-06 in a fresh worktree, where `lab:fetch` returned exit 0 having reached no host at all, and the
# file already sitting in `runs/fetched/` from an earlier run made it look like it had worked.
#
# That is `fleet:deploy` exiting 0 having deployed to zero hosts, which cost this project a silent
# no-op deploy on the same day, arriving in the lab commands by the same door: `inventory.yml` is
# gitignored (#54), so any checkout that has not been given one resolves nothing.
#
# THIS PLAY RUNS ON `localhost`, which is always available, precisely so it can speak when the real play
# cannot. A guard living inside the `a11y_lab` play would be skipped by the very condition it exists to
# report — the bootstrap shape `inventory-install.yml` already had to solve.
- name: The lab must be in the inventory before anything claims to have run
  hosts: localhost
  gather_facts: false
  tasks:
    - name: The lab must be in the inventory
      # ONE SPELLING, shared with every other playbook here -- see the include for what a
      # zero-host run costs and why the remedy names a machine (#1980).
      ansible.builtin.include_tasks: tasks/require-inventory-group.yml
      vars:
        inventory_group: a11y_lab
        inventory_group_noun: lab
    - name: The control-plane key must be supplied
      ansible.builtin.include_tasks: tasks/require-control-plane-key.yml

- name: Run a lab job
  hosts: a11y_lab
  gather_facts: false
  vars_files:
    - vars/lab-catalogue.yml

  vars:
    # Every `-e NAME=` a caller may supply. NOT derived: this is the published interface, and it is what
    # makes a name a job ignores answerable rather than silently dropped. A name absent from this list is
    # outside the interface and Ansible will discard it — there is no way to enumerate extra vars, so this
    # list is the boundary of what can be checked at all, and adding a parameter means adding it here.
    lab_caller_params: [only, out, exclude, role, shard, model, worker, workers, sample, subtype, feature, capture_root, captures_dir, apply, case]
    # WHICH HEADS `-e exclude=` MAY LEAVE OUT OF A TRAIN, by NAME. The value is a key of this map and never a
    # subtype: the subtype strings live here, in a tracked file, so `-e exclude=4.1.3:status-progress` and
    # `-e exclude=../x` are inexpressible rather than rejected -- the containment `out` and `worker` use.
    #
    # THIS IS AN EXPERIMENT LEVER, NOT A DECISION. It does not touch `rule-ownership.json`, so who decides a
    # subtype is unchanged for every other run; the trainer stamps the model it produces `releaseEligible:
    # false` and names what it lacks (`excludedSubtypes`), so it cannot pass for a full one. Add a name when
    # an isolating retrain needs another head left out, and pin it in `lab-job.test.ts`.
    lab_exclude_flag: '--exclude-subtype='
    lab_train_exclusions:
      # #2258's cheapest isolating candidate: 24 positives each, 0 of 24 at their own operating point.
      status-heads: ['4.1.3:status-progress', '4.1.3:status-waiting']
    # THE SHAPE OF AN `-e only=` VALUE, stated once because two jobs now take one. Ids are `[a-z0-9.+-]`
    # and commas, so a path, a flag or a shell metacharacter is INEXPRESSIBLE rather than rejected — the
    # containment `isValidCaptureId` uses, and the reason every parameter here is a name and not a path.
    # A trailing `+` is how this corpus spells a whole family (`route-title-stale+`).
    #
    # A variable rather than the literal repeated in each assert: `capture-only` and `evidence-check` are
    # validating the SAME parameter to the SAME shape, and this repo's record on a fact stated twice is
    # that the copies drift. Each job keeps its own `fail_msg`, because what to do about a refusal is not
    # the same in both.
    lab_case_id_shape: '^[a-z0-9][a-z0-9.+-]{0,80}[+]?(,[a-z0-9][a-z0-9.+-]{0,80}[+]?)*$'
    # A parameter that reaches its command through a DERIVED FACT rather than by its own name. The
    # derivation below reads the job's own command, and a command saying `{{ lab_named_worker }}` cannot
    # say which parameter produced it — so the one indirection is declared, with its reason, rather than
    # guessed at. `lab-job-params.test.ts` refuses a new `set_fact` guarded by `when: <param> is defined`
    # that is not listed here, which is what stops this drifting from the tasks that build the facts.
    #
    # THE VALUE IS AN EXPRESSION, NOT JUST A NAME, and that is what lets an alias be OPTIONAL. The
    # derivation decides required-versus-optional by looking for `| default(...)` or `is defined`, so a
    # bare name could only ever derive REQUIRED — true of the one alias here and wrong for the next one.
    #
    # READ BY `lab-job.test.ts`, which derives its substitution table from THIS map. It carried its own
    # hardcoded copy until 2026-09-06, and the comment here cited `lab-job-params.test.ts` as what kept
    # the two in step — **a file that has never existed in this repository**. The fact was stated twice,
    # nothing compared the copies, and the sentence saying otherwise is what stopped anyone checking.
    lab_param_aliases:
      # `-e worker=` is a NAME from the inventory and the command needs an ADDRESS. Turning one into the
      # other is the containment: an address cannot be supplied directly.
      lab_named_worker: worker
      # `-e workers=` names WHICH boxes and defaults to all of them. Same containment, same reason: the
      # value is a list of inventory HOST NAMES (or a count), never addresses, so every address in the
      # result is still built from the inventory and still passes the strict address assert below.
      lab_selected_workers: workers | default('the whole fleet')
      # `-e capture_root=` names WHICH captures directory, inside the dataset root. Aliased for the
      # same reason as the two above: the deriver reads requiredness out of the fact's expression,
      # so a parameter reaching a job only through `setenv` is invisible to it without this line.
      lab_capture_root: capture_root | default('')
    # Every job this playbook can run. `timeout` becomes `RuntimeMaxSec`, so it is a real ceiling rather
    # than advice — set it above the slowest honest run, because a deadline that expires early turns "still
    # working" into "failed".
    lab_jobs:
      train:
        params: {out: optional, exclude: optional}
        # Trains to a SCRATCH directory on purpose. The trainer now refuses to overwrite a release-eligible
        # model without `--allow-overwrite`, and promoting is a separate, deliberate step — one was already
        # lost to a `git checkout` on the assumption that a tracked file is always recoverable.
        # `-e out=<name>` names WHICH scratch directory, from a fixed list. A path is not expressible.
        #
        # It was hardcoded to `runs/model-candidate`, which made the job single-use: the trainer refuses to
        # overwrite a release-eligible model, so the SECOND train always failed with "REFUSING to
        # overwrite". Measured 2026-08-22 — the multi-defect retrain died on it in 1.9 s.
        #
        # The fix is not `--allow-overwrite`. That guard exists because a model was lost once, and a job
        # that always passes the flag is a guard nobody has. Naming the output keeps every candidate,
        # which is also what makes a comparison possible: shipped vs candidate vs multidefect, side by
        # side, rather than each one destroying its predecessor.
        argv: ["{{ lab_python }}", "packages/lab/scripts/train-screenreader-model.py",
               "--data", "runs/screenreader-dataset/with-realism.jsonl",
               "--output", "runs/model-{{ out | default('candidate') }}"]
        timeout: 3600
        # `-e exclude=<name>` leaves a NAMED GROUP of heads out of this run (`lab_train_exclusions`), for an
        # isolating retrain. APPENDED to `argv` above by the include below, so a train naming no `exclude`
        # runs exactly the argv it always did and `argv` stays a plain list every reader of this catalogue
        # (`lab-fetch-paths.test.ts` reads its `--output`) can index. `is defined` is the spelling
        # `lab-job.test.ts` derives OPTIONAL from, which keeps `params:` and this expression agreeing.
        #
        # No literal `--exclude-subtype=` in here, on purpose: the derivation reads names out of `{{ }}`,
        # and `subtype` is a published parameter, so that flag spelled inline would make this job appear to
        # take `-e subtype=`. The spelling lives in `lab_exclude_flag`, beside the groups it prefixes.
        argvAppend: "{{ (lab_train_exclusions[exclude] | map('regex_replace', '^', lab_exclude_flag) | list)
                        if exclude is defined else [] }}"
      build-realism:
        argv: ["/usr/bin/node", "packages/lab/scripts/build-realism-tier.mjs",
               "--out=runs/screenreader-dataset/with-realism.jsonl"]
        timeout: 900
      # The one check that scores REAL pages through the PRODUCT path. It measured the shipped model and
      # only the shipped model, so the question it exists to answer -- "is this CANDIDATE safe to ship?" --
      # could be asked only after promoting, which is the wrong order and the same shape as `npm run eval`
      # resolving the shipped artefact always.
      sweep:
        params: {model: optional}
        argv: ["/usr/bin/node", "packages/lab/scripts/calibrate-abstention.mjs"]
        setenv: ["A11Y_SCORER_MODEL={{ ('/opt/a11y/runs/model-' ~ model) if model is defined else '' }}"]
        timeout: 1800
      # WHICH records does a head fire on wrongly, and what do they have in common? A report says "36 false
      # positives" and cannot distinguish "the head lost a veto that was doing real work" from "something
      # specific changed about one group of pages" — opposite fixes, and the second is invisible to every
      # summary statistic.
      false-positives:
        params: {out: optional}
        argv: ["{{ lab_python }}", "packages/lab/scripts/diagnose-false-positives.py",
               "--model", "runs/model-{{ out | default('candidate') }}",
               "--data", "runs/screenreader-dataset/with-realism.jsonl",
               "--out", "runs/model-{{ out | default('candidate') }}/false-positives.json"]
        timeout: 1800
      # The veto audit, against a NAMED candidate rather than the shipped weights. `release:gate` runs the
      # same audit but always on what is shipped, which cannot answer "did this retrain help?" — the
      # question a candidate exists to settle. Reads files already on disk; no worker, seconds.
      shortcuts:
        params: {out: optional}
        # `npm run`, not the interpreter directly, so the unclosable-veto map is emitted first — the
        # audit reads `runs/unclosable-vetoes.json` to tell a veto worth corpus work from one nothing can
        # close, and that file is gitignored and derived. A box that never emitted it would forgive
        # nothing and report a longer work list than exists.
        #
        # The chain lives in the npm script, exactly as `corpus:grants-audit` puts its `corpus:grants-map`
        # there. NOT `/bin/sh -c` here: this file's own rule is that a shell reintroduces the quoting
        # class that sent four capture shards at `--worker=http://:8765` for 29 minutes.
        argv: ["/usr/bin/npm", "run", "--silent", "scorer:shortcuts", "--",
               "--model", "runs/model-{{ out | default('candidate') }}",
               "--data", "runs/screenreader-dataset/with-realism.jsonl",
               "--no-baseline"]
        timeout: 900
      # RECORD the veto baseline, against the SHIPPED weights. A separate job from `shortcuts` because it
      # WRITES a claim that every veto it records is understood and accepted -- the same reason
      # `rules-real-pages-update` is separate from `rules-real-pages`. Never a side effect of auditing.
      # THROUGH THE NPM SCRIPT, because the classification it records is DERIVED and this job was the one
      # place that skipped deriving it. `scorer:shortcuts` emits `runs/unclosable-vetoes.json` first; this
      # called the Python directly, so the authoritative baseline -- tracked, and a release gate -- was
      # written against whatever map happened to be on the lab's disk. The audit refuses an ABSENT map and
      # cannot see a STALE one, so the failure is silent by construction. Measured 2026-08-30: an entry
      # extended from 5 features to 10 landed in git, the baseline was re-recorded, and the recorded
      # classification did not move. Same rule as `corpus:grants-audit`: the chain lives in the npm script.
      shortcuts-baseline:
        argv: ["/usr/bin/npm", "run", "--silent", "scorer:shortcuts:baseline"]
        timeout: 900
      # THE SAME DELIBERATE ACT, against the CANDIDATE, and without it a new head can never be promoted.
      #
      # `candidate:gate` blocks any head missing from the baseline — deliberately: "it still blocks, and
      # the day ownership moves the veto is waiting". The only route to writing the baseline was
      # `shortcuts-baseline`, which reads the SHIPPED weights, and a new head is by definition not in
      # them. So the two requirements were circular and the first new head in a long time (3.3.3, added
      # 2026-09-02) could not get past promote in either direction.
      #
      # Kept as a SEPARATE job rather than a flag on the other, because the difference is which model's
      # vetoes you are asserting you have read and accepted, and that is exactly the thing a reader of the
      # job list should not have to infer.
      shortcuts-baseline-candidate:
        argv: ["/usr/bin/npm", "run", "--silent", "scorer:shortcuts:baseline:candidate"]
        timeout: 900
      acceptance:
        params: {out: optional}
        argv: ["{{ lab_python }}", "packages/lab/scripts/evaluate-screenreader-acceptance.py",
               "--data", "runs/screenreader-acceptance/repeat-1.jsonl",
               "--data", "runs/screenreader-acceptance/repeat-2.jsonl",
               # The disjointness proof must be against what the model was actually TRAINED on. Defaulting
               # to the base dataset means it never examines the realism tier.
               "--training-data", "runs/screenreader-dataset/with-realism.jsonl",
               # All three follow `out`, or the gate grades one model and files its verdict under another.
               # Half-parameterising this is worse than not doing it: the report would LOOK like it belongs
               # to the new candidate while describing the old one.
               "--model", "runs/model-{{ out | default('candidate') }}",
               "--training-report", "runs/model-{{ out | default('candidate') }}/training-report.json",
               # NO `--allow-ineligible` here, deliberately. It was needed until 2026-08-23, when the
               # eligibility guard lived in `verify_artifact` for every caller alike: the trainer marks a
               # fresh candidate ineligible BECAUSE acceptance has not run, and the evaluator refused any
               # ineligible model — so the gate that would qualify a candidate declined to run on an
               # unqualified one. The fix is at the call site, where the evaluator declares it does not
               # require eligibility, rather than here where a job would pass a flag every single time.
               "--out", "runs/model-{{ out | default('candidate') }}/acceptance-report.json"]
        timeout: 1800
      # THE REAL-PAGE CORPUS, across the WHOLE FLEET — same as `capture` above, and for the same reason.
      #
      # This took a single `--worker` and captured 77 pages serially while three identical boxes sat idle:
      # ~6.5 h of a ~1.6 h job. The escape hatch was `--shard=i/n` launched by hand N times, which is not
      # parallelism so much as parallelism the operator must perform correctly every time — and it is the
      # exact surface that once sent four shards at `--worker=http://:8765` for 29 minutes.
      #
      # `A11Y_WORKERS` comes from `lab_fleet_workers`, derived from `inventory.yml`, so adding a box to the
      # fleet makes it participate with no change here. That is the property a per-shard job entry could
      # never have: four hardcoded shards is a fleet size written down in a second place, and this repo's
      # record on facts stated twice is that they drift.
      #
      # Politeness does NOT depend on how many workers run: `host-throttle.mjs` keys the gap on the
      # publisher, so each site sees the same rate at any fleet size. `--shard` still works, for splitting a
      # run across separate machines; it is no longer how you use more than one worker.
      capture-real-pages:
        progress: runs/real-page-corpus
        params: {role: optional, shard: optional}
        argv: ["/usr/bin/node", "packages/lab/src/training/capture-real-pages.mjs",
               "--role={{ role | default('training') }}",
               "--shard={{ shard | default('0/1') }}"]
        setenv: ["A11Y_WORKERS={{ lab_fleet_workers }}"]
        # 77 real pages on one worker measured ~6.5 h; across the fleet ~1.6 h. Generous either way, and a
        # ceiling rather than a cost.
        timeout: 28800
      # CAPTURE ONE SUBTYPE, to prove a corpus change works before paying for the whole thing.
      #
      # A full recapture is ~4 h on this fleet, and a change usually targets ONE subtype's evidence. Paying
      # four hours to discover the fix did not move the number is the wrong order, and it is the order this
      # catalogue enforced by having no smaller unit: `capture` was all-or-nothing, while
      # `capture-screenreader-dataset.mjs` has supported `--only=` with exact ids and comma-separated lists
      # the whole time. The capability existed; no job exposed it.
      #
      # `-e only=<ids>` takes what `check-signals` and the audits already PRINT — a case id, or a list of
      # them — so the natural next step after a gate names failures is to paste them back in. Substring
      # matching means `-e only=route-title-stale` sweeps that family, which is the common case.
      #
      # Contained by SHAPE like every other parameter: ids are `[a-z0-9.+-]` and commas, so a path or a
      # flag cannot be expressed. The runner validates it a second time and refuses an id matching nothing,
      # which is the difference between "captured 0 cases" and "you asked for a case that does not exist".
      capture-only:
        progress: runs/screenreader-dataset
        params: {only: required, workers: optional, capture_root: optional}
        # `:fresh` — GENERATE, then capture. It ran `training:capture` alone, so a case that did not yet
        # exist as a page could never be captured: `training:capture failed: No generated case matches`.
        # That defeats half of what this job is for. `--pipeline=verify` exists to prove a corpus change
        # cheaply, and ADDING a case is the corpus change most worth proving before paying for the whole
        # corpus. The `capture` job has always used `:fresh`; this one was the odd one out, against this
        # repo's own rule that "capturing without regenerating is testing the previous commit".
        # `--no-cache`, and WITHOUT it this job could not do the thing it exists for.
        #
        # Captures are cached, so `training:capture --only=<case>` on a case that already has one returns
        # the STORED capture and reports success. A job whose stated purpose is "prove a corpus change
        # cheaply" then proves nothing: it re-serves the evidence from before the change.
        #
        # Found 2026-09-01 chasing a `grants-audit` failure. One capture of
        # `headings-none-refunds+also-filename-alt` was pathological -- 330 s and every sweep silent -- and
        # this job "reproduced" it twice, which read as a page defect and sent me hunting one. It was the
        # cache both times. Captured with `--no-cache` against the same fleet the same minute: 22 s,
        # 31 lines, `graphics 1/1`, `observed.graphics.complete: true`. The page was always fine.
        #
        # Cheap, because this job is bounded to a handful of cases by `--only`; the corpus-wide runs keep
        # their cache. Same rule the acceptance runs already follow -- `DATASET_KIND=acceptance` refuses
        # the cache outright, "because those runs exist to test whether NVDA's output is still stable",
        # which is exactly what this one is for.
        # `workers` CAPS THE POOL, and it exists so a SCALING measurement is dispatchable at all.
        #
        # Without it this job runs across every box in `lab_fleet_workers`, so "the same shard at five
        # boxes and at ten" -- the only way to establish whether this fleet scales, and the measurement the
        # hardware decision rests on -- could not be expressed. Found 2026-09-06 when that run was approved
        # and turned out to have no way to ask for the second arm. The alternative was `-e` an unread
        # variable and hope, which `lab-job.yml`'s own parameter guard refuses on purpose.
        #
        # It TRUNCATES the address list rather than setting `A11Y_MAX_WORKERS`, which is a host-memory
        # override for the local UTM path and does not apply to bare metal. Truncating names exactly which
        # boxes ran, so the arm is reproducible and `A11Y_WORKERS` still reads as the pool the run used --
        # a run that cannot say which boxes it measured is not a comparison.
        argv: ["/usr/bin/npm", "run", "training:capture:fresh", "--", "--only={{ only }}", "--no-cache"]
        # `lab_selected_workers`, the asserted fact built above — the ONLY job that reads the selection.
        # Deliberately not applied to the other pooling jobs: a corpus recapture on a subset is a corpus
        # captured on fewer boxes with nothing recording that, and this job is bounded to a handful of
        # cases by `--only`, which is what makes a deliberately-smaller run meaningful here and a
        # silent-partial-corpus hazard there.
        #
        # THE SLICE USED TO BE INLINE HERE and this replaced it when names arrived: an inline expression
        # can only ever cut the address string it is handed, so selecting BY NAME needs `hostvars`, which
        # means a fact — and a fact is what can be put through the address assert. The derivation still
        # reads `workers` as OPTIONAL because `lab_param_aliases` maps this fact to
        # `workers | default(...)`, which is why that map's values are expressions rather than names.
        # `DATASET_CAPTURE_ROOT` EXISTS IN THE CODE AND NO JOB EXPOSED IT — the same shape as `--only=`,
        # which `capture-screenreader-dataset.mjs` supported for months while the only unit available was
        # all-or-nothing. `dataset-paths.mjs` has read it since forever; nothing could set it from here.
        #
        # WHY A SCALING RUN NEEDS IT, measured 2026-09-06. #22 pre-registers "per-capture median and IQR at
        # each arm" and mandates ALTERNATING arms — and both arms wrote to the same `captures/`, so the
        # second overwrote the first's per-capture diagnostics before anything could read them. **The
        # method destroyed the statistic the row asks for**, and alternating makes it worse rather than
        # better. The first run could only report wall-clock-derived figures, which is a proxy.
        #
        # Empty by default, so every existing caller is byte-identical: `DATASET_CAPTURE_ROOT=` unset is
        # what `dataset-paths.mjs` already falls back on (`|| "captures"`). A NAME from a fixed shape, never
        # a path, for the reason every other parameter here is contained that way.
        setenv: ["A11Y_WORKERS={{ lab_selected_workers }}", "DATASET_CAPTURE_ROOT={{ lab_capture_root }}"]
        # Minutes, not hours: this exists precisely because it is small.
        timeout: 3600
      # `--require-complete` because THIS box owns the corpus. Here "no evidence for that case" is a real
      # answer — go capture — rather than the unanswerable question it is on a laptop, where `runs/` is
      # gitignored and a copy is only ever as fresh as its last sync.
      check-signals:
        argv: ["/usr/bin/node", "packages/lab/src/training/check-signals.mjs", "--require-complete"]
        timeout: 900
      # THE SYNTHETIC CORPUS, across the WHOLE FLEET. `A11Y_WORKERS` comes from `lab_fleet_workers` below,
      # which is built from the inventory and asserted against a strict pattern — never from the caller, for
      # the reason in this file's header.
      #
      # Pooled because that is the only way the hardware gets used: the runner already dispatches one case
      # per worker from a shared queue, evicts a worker that fails three in a row and hands its cases back.
      # 2,124 captures at the ~30 s each these boxes measure is ~17.7 h on one worker and ~4.4 h on four.
      # GENERATES THE PAGES FIRST, and that is not a convenience.
      #
      # This entry ran `capture-screenreader-dataset.mjs` alone, so it captured whatever pages happened to
      # be on the lab's disk. Measured 2026-08-22: a change that made 808 of 1,061 pairs stale locally was
      # dispatched here, the job pulled the new commit, captured the OLD pages, hit cache on every case and
      # reported success in 13 seconds. A full recapture reported as done, having done nothing — the
      # silent-no-op shape this repo keeps paying for, one layer out from the code that was fixed today.
      #
      # `training:capture` is not enough on its own for the same reason `worker:deploy` is not "copy the
      # files": the artifact under test has to be REBUILT from the checkout the job pulled, or the job is
      # testing the previous commit. Generation is idempotent — same code, same bytes — so this costs
      # nothing on a run where the pages were already current, including a `--resume`.
      capture:
        progress: runs/screenreader-dataset
        argv: ["/usr/bin/npm", "run", "training:capture:fresh"]
        setenv: ["A11Y_WORKERS={{ lab_fleet_workers }}"]
        # Above the ~4.4 h a full recapture measures, because a ceiling that expires early turns "still
        # working" into "failed" — and this is the most expensive job in the catalogue to lose.
        timeout: 28800
      # THE WHOLE RETRAIN, as one job. Until 2026-08-23 this sequence existed nowhere — producing a
      # candidate meant running eight things in order, and the ORDER and the STOP CONDITIONS lived in
      # somebody's head. Every defect found that day existed because the pipeline had never once been run
      # end to end; a human silently works around what a script cannot.
      #
      # It STOPS at the first failing gate rather than carrying on, because a pipeline that continues past
      # `check-signals` produces a candidate trained on a corpus with a hole in it, and the number at the
      # end looks exactly like a good one.
      retrain:
        # A COMPOSITE job DOES capture, for part of its life. Declaring `progress:` here was wrong until
        # `training:status` learned `--since`, because after the capture stage this file describes a
        # FINISHED run for every hour the job spends exporting, training and running gates -- and
        # "running: false, 49 of 49" read as a finished JOB is the misread that destroyed 12 in-flight
        # captures on 2026-09-05. `lab-status.yml` bounds the read to this invocation's start, so the
        # file is reported during the capture and refused after it.
        progress: runs/screenreader-dataset
        argv: ["/usr/bin/npm", "run", "lab:retrain"]
        setenv: ["A11Y_WORKERS={{ lab_fleet_workers }}"]
        # A full corpus capture is the long pole; the rest is minutes. Above the ~9.8 h a full recapture
        # measures on this fleet, because a ceiling that expires early turns "still working" into "failed".
        timeout: 50400
      # THE ACCEPTANCE INSTRUMENTS — generate, capture, export. They had no job at all until 2026-08-23,
      # which is the third pipeline step found this week reachable only by hand (promotion and artifact
      # reading were the others). The held-out set is what decides whether a candidate ships, so the way it
      # is produced should be at least as reviewable as the corpus it is compared against.
      generate-acceptance:
        argv: ["/usr/bin/npm", "run", "training:generate-acceptance"]
        timeout: 900
      # NEVER CACHES, by design: `DATASET_KIND=acceptance` refuses reuse, because these runs exist to test
      # whether NVDA's output is still stable. So this is a full capture of every acceptance page every
      # time — 42 pairs, and TWICE because the two repeats are what measure that stability.
      #
      # TWO ENTRIES RATHER THAN A `repeat` PARAMETER, and the ugliness is the point. `REPEAT` reaches the
      # child's ENVIRONMENT, and this catalogue's rule is that env additions come from here and never from
      # a caller's extra var — because `A11Y_PYTHON` is read at four sites and becomes the executed
      # interpreter, so a job that interpolates caller input into env is arbitrary code execution with a
      # whitelisted job name. `run-job.test.ts` enforces it and caught this within the hour.
      #
      # A validated enum would have been safe in fact and wrong in shape: the guarantee is that NOTHING
      # from the wire reaches the environment, and an exception that happens to be safe today is an
      # exception somebody widens tomorrow.
      capture-acceptance:
        progress: runs/screenreader-acceptance
        argv: ["/usr/bin/npm", "run", "training:capture-acceptance"]
        setenv: ["A11Y_WORKERS={{ lab_fleet_workers }}", "REPEAT=repeat-1"]
        timeout: 14400
      capture-acceptance-2:
        progress: runs/screenreader-acceptance
        argv: ["/usr/bin/npm", "run", "training:capture-acceptance"]
        setenv: ["A11Y_WORKERS={{ lab_fleet_workers }}", "REPEAT=repeat-2"]
        timeout: 14400
      # EVERY repeat, in one job, because the acceptance evaluator reads every repeat.
      #
      # It was two jobs -- `export-acceptance` (repeat-1) and `export-acceptance-2` (repeat-2) -- with
      # nothing relating them, feeding one consumer that reads both. On 2026-08-24 that produced exactly the
      # drift it invites: repeat-1 rebuilt under the current contract, repeat-2 left from the previous day,
      # and a held-out score computed half on each. A number like that looks authoritative and is not,
      # which defeats the only purpose a held-out gate has.
      #
      # A job whose output is a SUBSET of what its consumer reads is a drift generator. The fix is not to
      # remember the second command.
      # PROMOTION RUNS WHERE THE CANDIDATE IS, which is here and not on a laptop.
      #
      # `promote:model` copies the trained weights into `packages/scorer/models/` and writes the changeset.
      # It could only ever run in a checkout that HAS the candidate -- and the candidate is trained on this
      # box, under `runs/`, which is gitignored. So the only routes were copying the weights to a laptop by
      # hand, or promoting without having tested the pair. Both were the reason nothing had been promoted.
      #
      # This runs the candidate gate FIRST, on the machine where the v14 code and the v14 weights both
      # exist, and promotes only if it passes. That is the thing that was impossible before: testing both
      # halves TOGETHER before shipping either.
      #
      # `promote-model.mjs` refuses when a previous promotion is still uncommitted where this one would
      # write — added 2026-08-27, having been asserted here and in CLAUDE.md while no code ran it.
      # Committing and pushing
      # is deliberately NOT here: promoting is a MAJOR release of `@a11ign/scorer` (ADR 0007), and a
      # job that can push a release is a job that can release by accident. `lab:job -e job=promote-diff`
      # shows what it produced; a human commits it.
      # NO SHELL. `argv:` with npm, never `/bin/sh -c`, because this repo's own rule is that a shell here
      # reintroduces the quoting class that sent four capture shards at `--worker=http://:8765` for 29
      # minutes. The gate-then-promote chain lives in an npm script where it can be read and tested.
      promote:
        params: {out: optional}
        argv: ["/usr/bin/npm", "run", "--silent", "promote:gated", "--",
               "--from={{ out | default('candidate') }}"]
        timeout: 3600
      # What the promotion actually changed, so it can be reviewed before anyone commits it.
      promote-diff:
        argv: ["/usr/bin/git", "status", "--short", "--", "packages/scorer/models", ".changeset"]
        timeout: 300
      export-acceptance:
        argv: ["/usr/bin/npm", "run", "training:export-acceptance:all"]
        timeout: 1800
      # EXPORT FOR A TEST, accepting captures whose page has moved.
      #
      # The normal export drops a capture that does not describe the current page, which is right for a
      # release and wrong for a question. Measured 2026-08-26: a corpus change left 1,082 of 1,401 pairs
      # stale, so an export produced 319 and a retrain on it would have been a worse model that every gate
      # scored as whole. The only way to ask "did the fix work?" was to spend four hours first.
      #
      # The model this produces is structurally unpromotable: every record is stamped `grade: "test"`, the
      # trainer sets releaseEligible False at initialisation and says why, and promote-model refuses a
      # model that is not eligible. Three gates, none of them a comment.
      export-test:
        argv: ["/usr/bin/npm", "run", "training:export:test"]
        timeout: 900
      # REGENERATE THE PAGES AND THE MANIFEST, and nothing else. The one job the catalogue was missing,
      # and its absence was found by the guard that needs it: `export` refuses a manifest whose case
      # DEFINITIONS no longer match `CASES`, and its refusal named a raw
      # `node packages/lab/src/training/generate-screenreader-dataset.mjs` -- a command an operator
      # dispatching jobs cannot run. This repo's own rule: a remedy nobody can reach is a remedy nobody
      # applies.
      #
      # Measured 2026-09-06: adding `alsoFails: ["2.4.7:focus-removed-on-receipt"]` to nine cases changed
      # no page byte and no capture, and `export` correctly refused every later stage until the manifest
      # caught up. The only route to that refresh was a CAPTURE job -- `capture` (a ~4.4 h full corpus) or
      # `capture-only` (which is `--no-cache` by design, so it pays for a real capture to do it). Paying
      # for evidence to refresh a label is the wrong trade.
      #
      # Deliberately NOT folded into `export`. Which of the manifest and `CASES` is authoritative is
      # genuinely ambiguous -- that is why the guard reports rather than picks -- so an export that
      # silently regenerated would resolve the ambiguity by fiat and destroy the only signal that a case
      # matrix and a captured corpus have diverged.
      #
      # Generation is idempotent: same code, same bytes, so a page file is rewritten byte-identically
      # unless the page actually changed. Running this cannot invalidate a cached capture on its own.
      generate:
        argv: ["/usr/bin/npm", "run", "training:generate"]
        timeout: 900
      # Cheap, and here because it is a STEP rather than a check: it rewrites the labels every later stage
      # reads, and it silently used a stale manifest twice in one session before the drift guard existed.
      export:
        argv: ["/usr/bin/node", "packages/lab/src/training/export-screenreader-dataset.mjs"]
        timeout: 900
      # The gate that must pass before a corpus run, and the one that must pass after a rule change. Both
      # were run by hand today, which is the same reason `evidence-check` and `export` are here.
      #
      # `gate:stability` captures 6 canary pages 5 times each and compares by CONTENT, so it needs a worker
      # and takes ~25 min. ONE worker, REQUIRED, and the reason is this job's own rather than a shared one:
      # it asks whether the PIPELINE is deterministic, so a second guest is a second variable and the
      # question cannot be asked across a pool at all.
      #
      # This said "one worker, like evidence-check and for the same reason" (#1956). `evidence-check` moved
      # to the fleet pool and the comparison went stale where a reader meets it FIRST, 700 lines before the
      # refusal that says otherwise. It is stated on `stability`'s own terms now, because restating another
      # entry's design is what let it drift: since #1948 `evidence-check` takes an OPTIONAL `-e worker=`,
      # which is one box for a different reason — aiming a narrowed read at a named guest, not removing a
      # variable — so the comparison would now be wrong in a second way.
      stability:
        params: {worker: required}
        # fleetVerdict/exitCodeFor, same 0/1/2 contract as gateVerdict -- see rules-gate's entry above for
        # why this is declared rather than left to be inferred.
        exitMeanings:
          "1": >-
            a canary was found UNSTABLE -- repeated captures of the same unchanged page disagreed by
            content, real nondeterminism. Do not start a corpus run.
          "2": >-
            INCONCLUSIVE -- one or more canaries could not be judged (errored, or too few usable captures),
            which reduces the denominator rather than counting as stable. "Could not measure" and "it
            varied" need opposite responses; re-run before trusting a PASS or FAIL from this one.
        argv: ["{{ lab_tsx }}", "packages/lab/scripts/stability-gate.mjs", "--local"]
        setenv: ["A11Y_WORKER={{ lab_named_worker }}"]
        timeout: 5400
      # SCORE THE SHIPPED MODEL FROM A COPY, which is what `release:gate` needs and could not do.
      #
      # `training:evaluate-acceptance` passes neither `--model` nor `--training-report`, and both default
      # into `packages/scorer/models/` — tracked source. The evaluator REFUSES to stamp a verdict there,
      # correctly: "that record describes what was true when those weights shipped", and a dirty
      # `packages/` also stops the lab pulling, which is how it ran 17 commits behind for days.
      #
      # So `release:gate` could never pass as written — measured 2026-08-26, blocking at stage 5 with the
      # refusal naming its own remedy. This job IS that remedy: copy to `runs/`, score the copy. The copy
      # is made here rather than inside the script because a script that copies its own input into a
      # writable place is a script that can no longer refuse.
      acceptance-shipped-copy:
        argv: ["/usr/bin/npm", "run", "--silent", "training:evaluate-acceptance:shipped"]
        timeout: 1800
      # THE WHOLE CHAIN AS ONE SUPERVISED UNIT — corpus, model and gates, inside systemd.
      #
      # `lab:pipeline` sequences the same stages and it runs on a LAPTOP: each stage is a supervised job,
      # and the thing deciding what comes next is a node process on a developer's machine. Measured
      # 2026-08-26 — five local watchers were killed during one capture. Each time the systemd unit
      # survived exactly as designed and the ORCHESTRATION did not, so the stages after it never started.
      #
      # As a job this is one unit: it survives the ssh connection, the playbook, and the laptop. If the
      # local wait dies, `lab:status -e job=everything` still finds it running and `lab:log` still reads
      # what it said. That is the same argument ADR 0013 makes for jobs over shell commands, applied one
      # level up to the thing that sequences them.
      #
      # `lab:pipeline` stays and is still the right tool for a SHORT chain you want to watch — it prints
      # per-stage boundaries live, which a single unit cannot. This is for the long unattended one.
      everything:
        # A COMPOSITE job DOES capture, for part of its life. Declaring `progress:` here was wrong until
        # `training:status` learned `--since`, because after the capture stage this file describes a
        # FINISHED run for every hour the job spends exporting, training and running gates -- and
        # "running: false, 49 of 49" read as a finished JOB is the misread that destroyed 12 in-flight
        # captures on 2026-09-05. `lab-status.yml` bounds the read to this invocation's start, so the
        # file is reported during the capture and refused after it.
        progress: runs/screenreader-dataset
        argv: ["/usr/bin/npm", "run", "--silent", "lab:everything"]
        setenv: ["A11Y_WORKERS={{ lab_fleet_workers }}"]
        # Above a full recapture plus a train plus every gate, because a ceiling that expires early turns
        # "still working" into "failed" — and this is the most expensive unit in the catalogue to lose.
        timeout: 50400
      # THE COMPOSITE, and the only entry that delegates to an npm script rather than naming a binary.
      #
      # That is deliberate: `release:gate` IS the ordering -- isolation, then signals, then rules, then
      # held-out acceptance, then judge quality, cheapest first so a cheap failure does not wait behind an
      # expensive one. Re-listing the five here would duplicate that ordering into a second place, and the
      # two would drift; the thing being run is a sequence, not a program.
      #
      # `npm run <fixed name>` interpolates nothing, so it introduces none of the quoting risk the argv rule
      # exists to remove. Every component reads from disk and the venv -- none needs a capture worker, which
      # is why this can run while the fleet is doing something else.
      release-gate:
        argv: ["/usr/bin/npm", "run", "release:gate"]
        # eval:gate alone is 34 fixtures x EVAL_RUNS=3 against the local scorer.
        timeout: 5400
      # No worker: it scores the rule layer against captures already on disk. Seconds, and here because it
      # is what says whether the corpus still supports every boundary `rule-ownership.json` declares.
      # "Which rules have never fired?" — the general form of the defect that let 2.1.1 accuse 23 of 35
      # conformant pages. It needs the AUTHORITATIVE corpus: a partial copy means fewer fires, which reads
      # as less coverage, so a laptop cannot answer this and says so rather than guessing.
      # "Did this change make the tool accuse a conformant real page of something NEW?" rules:gate scores
      # the corpus and catches a rule going quiet; nothing caught the opposite until 2026-08-25, when
      # eleven separate defects had done exactly that with every gate green. Needs the AUTHORITATIVE
      # real-page corpus, which only the lab has.
      # THE TWO GATES THAT DRIVE WORKERS, RUN WHERE THE WORKERS ARE — control-plane-plan L1.
      #
      # These ran from a laptop, and that is where a transport fault came from: 9 of 40 responses lost
      # while the machine drove five boxes over Wi-Fi on a battery falling 18% to 1%. The workers are wired
      # and mains-powered and reported 0 failures across 242 captures throughout, so the only unreliable
      # thing in the path was the host doing the asking.
      #
      # What makes this possible is a fact nobody had checked: ADR 0012's credential split appears to pin
      # the laptop in place, because only it holds both fleet access and the `a11y-pve` key. But a GATE
      # reaches workers over HTTP :8765, not SSH -- measured from the lab 2026-08-29, all five answered 200.
      # The split governs deploy and provision, and has never governed this.
      #
      # No flags: both already default to every worker in `inventory.yml` and shard across the pool. And
      # the page server they lease now runs HERE, so the workers fetch corpus pages from a wired host --
      # moving the orchestration and leaving the traffic behind would be half a fix. `guestReachableUrl`
      # needs no change for that: it derives the reachable address from `os.networkInterfaces()`, so on the
      # lab it resolves to <the lab's address> exactly as it resolved to the laptop's address before.
      #
      # An hour each, because a gate is 8 canaries x 5 captures or 5 pages x 3, and a worker that has to
      # cold-start NVDA costs ~25 s more. The unit name is the lock, so a second dispatch is refused rather
      # than queued -- see lab-stop.yml if one must be ended.
      gate-stability:
        # Same script, same fleetVerdict/exitCodeFor contract as `stability` above -- see that entry.
        exitMeanings:
          "1": >-
            a canary was found UNSTABLE -- repeated captures of the same unchanged page disagreed by
            content, real nondeterminism. Do not start a corpus run.
          "2": >-
            INCONCLUSIVE -- one or more canaries could not be judged (errored, or too few usable captures),
            which reduces the denominator rather than counting as stable. Re-run before trusting a PASS or
            FAIL from this one.
        argv: ["{{ lab_tsx }}", "packages/lab/scripts/stability-gate.mjs", "--local"]
        # #1041: A NAMED WORKER, THE SAME AS `stability` ABOVE, AND FOR THE SAME STATED REASON -- this gate
        # asks whether the PIPELINE is deterministic, so a second guest is a second variable. The entry ran
        # the identical script with the identical flag and passed NO worker at all, so every dispatch died
        # on `no workers in A11Y_WORKERS and none in inventory.yml`. The script was right to refuse rather
        # than guess; the defect was here. Its recorded failures were read as the OS split and were never
        # that -- `gate:stability` itself passed through `stability` on the same code.
        params: {worker: required}
        setenv: ["A11Y_WORKER={{ lab_named_worker }}"]
        timeout: 3600
      # `--local` IS MANDATORY HERE AND IS NOT AN ESCAPE HATCH. `dispatchUnlessLocal` makes the control
      # plane the DEFAULT, which is right for an operator running the gate from a laptop -- but this entry
      # IS the control plane's dispatch, so without the flag the script dispatches again from inside the
      # job it was dispatched into. Measured 2026-08-30: `sh: 1: ansible-playbook: not found`, exit 127,
      # which reads like a broken lab rather than a job wired to call itself. Both dispatching gates need
      # it; `lab-job.test.ts` now discovers them rather than trusting this comment.
      gate-probe-order:
        # fleetVerdict/exitCodeFor, same 0/1/2 contract as rules-gate/stability above.
        exitMeanings:
          "1": >-
            probe order changed what was found on a page -- captured control/treatment/control and the
            treatment pass differed from both controls, a genuine order-dependence regression.
          "2": >-
            INCONCLUSIVE -- fewer pages were examined than this sweep's own population (corpus pages plus
            live sites, minus known movers); re-run rather than trusting the result shown.
        argv: ["/usr/bin/node", "packages/lab/scripts/gate-probe-order.mjs", "--local"]
        timeout: 3600
      rules-real-pages:
        # gateVerdict/exitCodeFor -- see rules-gate's entry above for why this is declared.
        #
        # EXIT 1 IS AN ASSERTION, NOT ANY NEW FINDING -- #1504, settling what #364 recorded. The two are not
        # the same claim: only an ASSERTED finding is the false-positive this project measures itself by,
        # and most rule-owned subtypes map `secondary` -> `cantTell`, so REFERRED is the common case by
        # design. Until #1504 this code fired on either, and a referral-only run refused release-gate stage
        # 11 (#915) under the script's own line saying it was not a publish blocker. A REFERRED-only reading
        # now prints a named WARNING and exits 0; `packages/lab/src/gates/referral-only-verdict.mjs` decides.
        exitMeanings:
          "1": >-
            a NEW ASSERTED finding appeared on a real, conformant page -- this tool stated that a page its
            publisher declares conformant fails a criterion (the OF THOSE N: ... ASSERTED line above says
            which), or a new finding has no recorded outcome and so cannot be called a referral. A
            REFERRED-only reading does not fire this code: it prints a WARNING and exits 0.
          "2": >-
            INCONCLUSIVE -- fewer real pages were examined than the audit's own population (an unusable
            page not already DECLARED in packages/lab/baselines/real-page-unexaminable.json still counts as a
            shortfall here, by design). Re-run rather than trusting the ratio shown.
        argv: ["/usr/bin/npm", "run", "--silent", "rules:real-pages"]
        timeout: 900
      # Same, with --update: rewrites the baseline. Separate job because it WRITES a claim that every
      # finding it records is correct, and that is a deliberate act, not a side effect of checking.
      rules-real-pages-update:
        # Same script as rules-real-pages above, but NOT the same exit contract: this path calls
        # `updateBaseline` directly (`process.exitCode = updateBaseline(current, pages)`), which returns
        # only 0 or 2 -- there is no gateVerdict/exitCodeFor here and no ASSERTED-vs-REFERRED "1", because
        # the --update path has no "asserted finding" state to signal (ceo's ruling, #1511, 2026-09-14
        # ~02:05Z). It WRITES the finding into the baseline regardless of which one it was, since its whole
        # purpose is recording the current findings -- see updateBaseline's own doc comment for the "0".
        exitMeanings:
          "2": >-
            INCONCLUSIVE -- fewer real pages were examined than the audit's own population; the baseline
            this writes would then describe a partial run. Re-run rather than trusting it.
        argv: ["/usr/bin/npm", "run", "--silent", "rules:real-pages", "--", "--update"]
        timeout: 900
      # #1614 needs axe-core run over the corpus's calibration pages "under #914's rules", and no route
      # existed: the catalogue had no job that installs a browser (the provisioning playbooks are for
      # WORKERS, and a hand install over ssh is the lab write `ceo` declined at #1626's 13:4xZ). The npm
      # packages (`@axe-core/playwright`, `playwright`) were already present; the browser and its apt
      # libraries were not (measured on the lab, #1626, Debian 12 x86_64).
      #
      # ONE COMMAND DOES BOTH HALVES. `playwright install --with-deps` downloads the pinned Chromium AND
      # apt-installs the system libraries it needs to actually launch (not just unpack) -- on Debian 12
      # this measured as libnss3, libnspr4, libatk1.0-0, libatk-bridge2.0-0, libdrm2, libxkbcommon0,
      # libxcomposite1, libxdamage1, libxfixes3, libxrandr2, libgbm1, libpango-1.0-0, libcairo2, libasound2
      # and fonts-liberation (#1626's own dependency audit; the exact set is Playwright's own for the OS it
      # detects, not a list this repo controls). IDEMPOTENT: a second run skips a Chromium already at the
      # pinned revision. FAILS LOUDLY if the download host is unreachable or apt cannot install: the
      # command's own non-zero exit is the ansible task's failure, with no wrapper swallowing it.
      #
      # WHAT IT WRITES, WHERE: the Chromium binary under `PLAYWRIGHT_BROWSERS_PATH`
      # (`/opt/a11ign/playwright-browsers`, pinned here rather than left at Playwright's per-user cache
      # default, because axe-calibration below must find the SAME install regardless of which account runs
      # it -- `lab-job.test.ts` asserts both jobs' `setenv` name the identical path). `axe-calibration.mjs`
      # reads `chromium.executablePath()` under this same variable as its own install marker (#1626): no
      # separate marker file to go stale independently of whether the binary is actually there.
      #
      # THE EXACT REMOVAL: `rm -rf /opt/a11ign/playwright-browsers`, then (only if nothing else on the box
      # needs them) `apt-get remove --purge` the package list above. Nothing here automates that removal --
      # apt state shared with the rest of the box is not this job's to touch on a whim.
      install-axe-browser:
        argv: ["/usr/bin/node", "node_modules/playwright/cli.js", "install", "--with-deps", "chromium"]
        setenv: ["PLAYWRIGHT_BROWSERS_PATH=/opt/a11ign/playwright-browsers"]
        timeout: 1800
      # Runs axe-core over #1614's own population (46 conformant calibration pages, derived from the
      # corpus's role/publishedClaim fields -- never a URL list, which `axe-calibration.test.ts`'s
      # #1626 MUTATION covers). REFUSES if `install-axe-browser` has not put a Chromium where this job
      # looks (the SAME `PLAYWRIGHT_BROWSERS_PATH` below). Writes one JSON to
      # `runs/axe-calibration/results.json`: per page, the URL, axe-core's version, the browser's version,
      # how long the page took, and its violated criteria and rule ids -- or, for a page axe could not
      # examine, that failure recorded on its own record rather than the page silently missing from the
      # file. Exit 1 means at least one page failed this way; re-run before trusting a file with any.
      axe-calibration:
        exitMeanings:
          "1": >-
            at least one calibration page could not be examined (a navigation failure, or axe itself
            throwing) -- its own record in the written JSON says which and why. The other pages' results
            are still written and still trustworthy; re-run to get a clean file before using this as a
            baseline.
          "2": >-
            REFUSED before any page was scanned -- either install-axe-browser has not put a Chromium at
            PLAYWRIGHT_BROWSERS_PATH (the printed message names the exact path checked), or a fatal error
            aborted the run before it could examine anything. Nothing was written.
        argv: ["/usr/bin/node", "packages/lab/scripts/axe-calibration.mjs"]
        setenv: ["PLAYWRIGHT_BROWSERS_PATH=/opt/a11ign/playwright-browsers"]
        timeout: 3600
      # WHAT STATE IS THE CORPUS IN? The question that had no command, so it was answered by opening an
      # SSH shell on this box and running ad-hoc Python -- about eight times on 2026-08-25, one of which
      # read the wrong field and reported `captured: 0` while `lab:status` correctly said 85.
      #
      # Reads files already on disk; no worker, seconds. It needs the AUTHORITATIVE corpus, which is why
      # it is a job at all: a laptop's `runs/` is gitignored and only as fresh as its last sync, and it
      # carries no `runs/model-*` -- so run there it answers a different question and says so.
      inventory:
        argv: ["/usr/bin/node", "packages/lab/scripts/lab-inventory.mjs"]
        timeout: 900
      # CAPTURES NO DECLARED PAGE CLAIMS — named and classified here, because only this box has the
      # authoritative corpus. A laptop's `runs/` is as fresh as its last sync and would report a set that
      # does not exist.
      #
      # REPORTS BY DEFAULT AND DELETES NOTHING. `-e apply=true` is a separate, deliberate act, the same
      # shape `lab:reset` takes and for the same reason: `runs/` is gitignored, these captures are hours of
      # worker time, and they are NOT reproducible -- `browserVersion` is a cache key, so evidence taken
      # under Edge 151 cannot be re-made now 152 ships. There is no undo.
      #
      # It refuses to delete a RELOCATED capture whatever you pass: those are declared pages reached at
      # another origin (every fixture is declared `localhost:5050` and captured at the LAN address, because
      # a worker cannot reach `localhost`), and five of them are the only real-page grounding 2.4.1, 2.4.2,
      # 2.4.3, 2.1.1 and 1.4.13 have. See #143 and #146.
      prune-orphan-captures:
        params: {apply: optional}
        # The flag is APPENDED or absent, never passed as an empty placeholder: `refuseUnknownFlags`
        # would reject a bare `--`, and a job whose no-op form is refused is one nobody runs.
        argv: "{{ ['/usr/bin/node', 'packages/lab/scripts/corpus-prune-orphans.mjs']
                  + (['--apply'] if (apply | default('') | string | lower) == 'true' else []) }}"
        timeout: 900
      # THE PYTHON TESTS, WHERE THE CORPUS IS. Several of them read `runs/` and SKIP HONESTLY without it,
      # so on a laptop and in CI they pass having examined a fraction of the data or none at all — the
      # same limitation `verify.corpus.test.ts` carries, and for the same reason.
      #
      # `test_applicability.py` is why this exists. A precondition that silences a true positive deletes
      # evidence outright, and it is invisible in every score: a finding never made cannot be counted as
      # missed. Verifying that against 104 held-out records and shipping would be the "validated on six
      # hand-picked cases" mistake exactly — it failed 44 cases in a live run last time.
      python-tests:
        argv: ["/usr/bin/npm", "run", "test:python"]
        timeout: 1800
      # THE APPLICABILITY AUDIT, with the interpreter the lab ACTUALLY HAS.
      #
      # `python-tests` above is the right job and it cannot do this one: the lab has no pytest, so it
      # reports "SKIPPED: no .venv/bin/pytest" and exits 0. Measured 2026-08-25 — an honest skip, and an
      # unverified precondition either way. A guard that can only run where the data is absent is not a
      # guard, and this one deletes evidence when it is wrong.
      # DOES A MULTI-DEFECT PAGE CARRY THE EVIDENCE ITS LABELS CLAIM?
      #
      # Every accompanying defect declares a `grants` feature and nothing read it -- eight declarations,
      # zero consumers. `check-signals` verifies the primary `badSignal` only, and `alsoFails` adds a label
      # with no signal, so a label for a defect whose evidence was never captured passed every gate.
      #
      # Emits the JS-side map first. The audit REFUSES without it rather than examining an empty set,
      # which is the failure it exists to catch.
      grants-audit:
        argv: ["/usr/bin/npm", "run", "--silent", "corpus:grants-audit"]
        timeout: 900
      # DOES NVDA EVER ANNOUNCE LEAVING A LANDMARK? The fact `vague_link_lacks_context` rests on and
      # nobody had established. Reports, never blocks: it describes NVDA, not a defect.
      # WHICH FEATURES ARE CONSTANT ACROSS A SUBTYPE'S POSITIVES — the corpus half of ADR 0015, answerable
      # with no weights at all. `scorer:shortcuts` reports which vetoes a head TOOK; this reports which it
      # COULD take, before a retrain exists to measure. It needs the authoritative export, which is why it
      # is a job: a laptop's copy predates the announcement parse and the audit refuses it by name.
      starvation:
        argv: ["/usr/bin/npm", "run", "--silent", "corpus:starvation"]
        timeout: 900
      container-exits:
        argv: ["{{ lab_python }}", "packages/scorer/python/audit_container_exits.py"]
        timeout: 900
      applicability-audit:
        argv: ["{{ lab_python }}", "packages/scorer/python/audit_applicability.py"]
        timeout: 900
      # HOW MANY FEATURE ZEROS ARE CAPTURE ARTEFACTS? Every structured feature is `float(bool(channel))`
      # and `any([])` is False, so a 0 means both "the page has none" and "nothing looked". `landmark_present`
      # was DELETED because 16 of its 16 zeros were truncated sweeps; no other channel has ever been measured.
      # A job because it needs the AUTHORITATIVE captures -- a laptop's copy answers `unknown` everywhere,
      # which is honest and says nothing about the pages. Reports, never blocks.
      observation-ambiguity:
        argv: ["/usr/bin/npm", "run", "--silent", "corpus:observation-ambiguity"]
        timeout: 900
      # WHY does a feature read what it reads on a subtype's positives? `scorer:shortcuts` says a head
      # vetoed one and `corpus:starvation` says one will be constant; NEITHER says why, and the three
      # possible answers -- definitional, a probe that never ran, a thin corpus -- need opposite work.
      # A job because it needs the AUTHORITATIVE export: a laptop's copy predates the parse and the
      # featurizer refuses it by name.
      explain-feature:
        params: {subtype: required, feature: required}
        argv: ["/usr/bin/npm", "run", "--silent", "scorer:explain-feature", "--",
               "--subtype", "{{ subtype }}", "--feature", "{{ feature }}"]
        timeout: 900
      # WHAT DID THE MODEL SEE on a held-out acceptance case? Its label, each head's score against its cut, and
      # the value of EVERY document feature by name -- so `orchestrator` can read whether the vetoed
      # `form_change_nonempty` / `validation_error_missing` were 0 on the 16 misses and 6 false alarms #2258
      # asks about (#2334). `explain-feature` cannot: it reads the TRAINING export, which does not hold these
      # cases. `scorer:explain --criterion=X` prints the ids this takes, ready to paste.
      #
      # `-e case=` is a case id or a comma-separated list, contained by the SAME shape as `capture-only`'s
      # (`lab_case_id_shape`), so a path or a flag cannot be expressed; a well-formed id the records do not
      # hold is refused by name by the reader, not printed as an empty block. `-e out=` names the model from the
      # fixed list, as `acceptance` does -- the reader takes its records from that model's own acceptance
      # report, so it reads the repeats the model was actually scored on. Read-only: no weights, no corpus.
      explain-case:
        params: {case: required, out: optional}
        argv: ["/usr/bin/npm", "run", "--silent", "scorer:explain", "--",
               "--model={{ out | default('candidate') }}", "--case={{ case }}"]
        timeout: 900
      rules-coverage:
        argv: ["/usr/bin/npm", "run", "--silent", "rules:coverage"]
        timeout: 900
      # HOW MANY REAL-PAGE CAPTURES LOSE REQUIREMENT 2's FULL-PAGE CLAIM, and to which sweep.
      # Reads `runs/real-page-corpus`, so it is a VERDICT only here: a copy in any other checkout is only
      # as fresh as its last sync, and one measured at 89 hours old reported cleanly over a corpus that no
      # longer existed. That is CLAUDE.md's "a gate that reads runs/ is not yours to report", and this
      # entry is what lets the rule be obeyed rather than worked around with a shell loop over SSH.
      full-page-claims:
        argv: ["/usr/bin/npm", "run", "--silent", "lab:full-page-claims"]
        timeout: 600
      rules-gate:
        # gateVerdict/exitCodeFor -- lab-job.test.ts derives which jobs need this and refuses a fifth
        # undeclared adopter, so it cannot go stale the way `evidence-check` alone did for weeks.
        exitMeanings:
          "1": >-
            a rule-owned criterion produced a false positive on a conformant corpus record -- a real
            regression, not a coverage shortfall.
          "2": >-
            INCONCLUSIVE -- fewer corpus records were examined than this rule's own declared population
            (gateVerdict's examined < of), almost always a stale or partial export. Re-export and re-run;
            do not read this as a pass.
        argv: ["{{ lab_tsx }}", "packages/lab/scripts/score-rules.ts", "--gate"]
        timeout: 900
      # THE SHIPPED MODEL against the SAME held-out set — the baseline without which a candidate's score
      # cannot be read. A candidate scoring 6 false positives is good or bad depending entirely on what the
      # model already in production scores, and until the acceptance set gained multi-defect cases nobody
      # could ask. Fixed path, no interpolation: this one always means the weights in the package.
      acceptance-shipped:
        argv: ["{{ lab_python }}", "packages/lab/scripts/evaluate-screenreader-acceptance.py",
               "--data", "runs/screenreader-acceptance/repeat-1.jsonl",
               "--data", "runs/screenreader-acceptance/repeat-2.jsonl",
               "--training-data", "runs/screenreader-dataset/with-realism.jsonl",
               "--model", "packages/scorer/models/screenreader-scorer",
               "--training-report", "packages/scorer/models/screenreader-scorer/training-report.json",
               "--out", "runs/model-shipped-baseline/acceptance-report.json"]
        timeout: 1800
      # POOLED, and this entry used to say the opposite — "single worker ON PURPOSE, because a second guest
      # is a second variable". That argument does not survive inspection: the BASELINE corpus was itself
      # captured across all four workers, so using one fresh worker does not remove the confound, it only
      # hides which side of the comparison it sits on. What actually defends the diff is fleet consistency,
      # which `fleet:status` proves — and which was itself inert until the browserVersion memo was fixed.
      # Check it is consistent before trusting a CHANGED verdict.
      # `argv` is a LIST, so the addresses are spliced in as
      # separate elements — joining them with a space would hand the script one argument containing four
      # URLs, which is the quoting class `argv:` exists to remove, reintroduced by hand.
      fleet-hours:
        # WHAT A CAPTURE RUN COST, summed per capture from each capture's own last cumulative `atMs`.
        # Reads the corpus and nothing else -- no fleet, no worker, no page server -- so it is safe to run
        # while a capture is in flight, though the number then describes a moving target and the tool's own
        # output says which corpus it read.
        #
        # IT MUST RUN HERE RATHER THAN ON A LAPTOP, which is the whole reason for this entry. `runs/` in any
        # checkout is a copy only as fresh as its last sync -- measured 2026-09-06 at 4,422 captures on
        # protocol 5 against a fleet on 16 -- so a median computed locally answers about a corpus that no
        # longer exists. That is this repo's most-recorded defect and the reason the board's fleet-hours
        # line reads "not instrumented" rather than carrying a laptop's figure.
        exitMeanings:
          "2": >-
            it billed NO capture and refuses to report a total it did not measure. Deliberately not 1:
            a cost report that examined nothing prints the same small number as a cheap run, so "0.00
            worker-hours" must never be a silent success. Read WHICH silence it names -- "no JSON found"
            and "walked N and billed none" need opposite fixes.
        # `-e dir=` targets ONE captures directory, which is what makes a per-arm scaling measurement
        # readable at all: #22's two arms now write to their own roots (`-e capture_root=`), and without
        # this the only thing `fleet:hours` could report is the whole corpus — every arm and every
        # protocol averaged into one number describing no run that ever happened.
        params: {captures_dir: optional}
        argv: ["/usr/bin/node", "packages/lab/scripts/fleet-hours.mjs", "--json",
               "--dir={{ captures_dir | default(lab_repo_path ~ '/runs') }}"]
        timeout: 1800
      # THE CORPUS'S ONLY DEFENCE, AND IT HAD NO JOB — added 2026-09-06 for issue #43.
      #
      # `corpus:snapshot` and `corpus:backup` are the two halves of the one thing that protects an asset
      # this project cannot rebuild: several thousand captures, 54 worker-hours measured, and NOT
      # reproducible -- `browserVersion` is a cache key precisely because Edge announces differently across
      # releases, so evidence taken under Edge 151 cannot be recreated now 152 ships.
      #
      # Both could only ever be run BY HAND, on the lab, over ssh. That is this repo's own rule failed on
      # the highest-value operation it has: *"an operation that matters is CLI-invocable and reviewable, or
      # it is done differently every time by whoever is at the keyboard."* `fleet:inventory-install` carries
      # the same sentence, and lost inventory cost a silent zero-host deploy the same day.
      #
      # THE DESTINATION IS DELIBERATELY NOT SET HERE. `corpus-backup.mjs` reads `A11Y_CORPUS_REMOTE` and
      # EXITS NON-ZERO without it rather than writing a local archive and reporting success -- "a backup
      # tool that reports success while leaving one copy on one disk is worse than no backup tool". A job
      # that supplied a default would be choosing where a user's data goes.
      corpus-snapshot:
        # Archives beside the corpus. Defends against `rm -rf runs/` and a bad recapture; NOT against
        # losing the machine, which is what `corpus-backup` is for. Reads the corpus and writes an
        # archive -- no fleet, no worker -- so it is safe beside a capture, though the archive then
        # describes a moving target.
        exitMeanings:
          "2": >-
            either there was nothing to snapshot, or -- the one that matters -- the archive holds FEWER
            JSON files than were on disk. It lists the archive back with `tar -tzf` and compares, because
            `tar` exiting 0 is not the archive holding the corpus: a 417 MB snapshot once extracted to
            4,959 of 5,445 files with no error anywhere. A short archive restores as a corpus that looks
            complete and is not, so this refuses rather than reporting a size. The archive is left in
            place to be inspected.
        argv: ["/usr/bin/npm", "run", "corpus:snapshot"]
        timeout: 3600
      corpus-backup:
        # Copies the newest archive off this machine and READS IT BACK to prove it arrived, over the
        # channel a restore would use -- the same argument as checking `/health.code` over HTTP rather
        # than through the deploy channel.
        exitMeanings:
          "1": >-
            either no destination is configured (`A11Y_CORPUS_REMOTE` unset on the lab) or the copy did
            not read back. Those need opposite work and the message says which: the first is a decision
            nobody has made, the second is a failed backup. Never treat it as "nothing to back up" --
            `npm run corpus:release` is the OTHER, already-working route (GitHub Releases on the private
            `a11ign/corpus-backups`, verified by download), unaffected by this job's own exit code (#1042).
        argv: ["/usr/bin/npm", "run", "corpus:backup"]
        timeout: 7200
      corpus-backup-verify:
        # A SEPARATE JOB RATHER THAN `-e verify=1`, and the test is why. The first version wrote
        # `{{ '--verify-only' if verify | default('') else '' }}`, and `lab-job.test.ts` refused it: the
        # deriver reads a parameter's REQUIREDNESS out of the argv, and a conditional expression is not a
        # shape it can answer for. That refusal is right beyond the mechanics -- CLAUDE.md records the SRE
        # Workbook's rule that a YAML+Jinja config accreting "ad hoc language features" becomes something
        # neither humans nor tools can analyse, and that the cleverness belongs in TOOLING. Two jobs whose
        # argv is a literal is the version an operator can read and a test can check.
        #
        # It is also a different QUESTION: "is the last backup still readable" is a check you want to run
        # on a schedule between backups, not a mode of writing one.
        exitMeanings:
          "1": >-
            the last backup is not readable at the destination, or no destination is configured. An
            unverified backup is a belief; a backup that fails THIS is a copy you cannot restore. Those
            two are not the same finding and this message is the only thing that tells them apart: "not
            readable" is a backup you cannot restore, "no destination" is a decision nobody made --
            `A11Y_CORPUS_REMOTE` is unset on the lab and the org settled it the other way, so it means
            THIS route has nowhere to look, not that the corpus is unprotected. `npm run corpus:release`
            (GitHub Releases on the private `a11ign/corpus-backups`, verified by downloading what it
            published) is the route that actually holds the corpus, and it verifies its own copy -- the
            same reason `corpus-backup`'s gloss above names it (#1042). Two hand-dispatches of this job
            against an unconfigured destination, 2026-09-10 and 2026-09-12, left a `failed` unit that
            read as a broken backup for eleven days (#2061).
        argv: ["/usr/bin/npm", "run", "corpus:backup", "--", "--verify-only"]
        timeout: 1800
      capture-check:
        params: {worker: required}
        # THE CAPTURE LAYER'S OWN REGRESSION CHECK, dispatchable at last. `capture:check` was documented as
        # MANDATORY after any change to `capture-core.mjs` and had never run once, because running it meant
        # a laptop with a route to a worker — so the one check written for the least testable code in this
        # repo was the one nobody could dispatch. `architecture-audit.md` named the gap; this closes it.
        #
        # A DIAGNOSTIC, DELIBERATELY, and that classification is load-bearing. It takes ONE named worker via
        # `--worker=` rather than reading `lab_fleet_workers`, so `captureBearingJobs` does not see it and
        # the fleet-staleness pre-check does not gate it — `worker-code-check.test.ts`'s own rule is that a
        # diagnostic must NEVER be the thing that takes the pool offline. A stale worker here costs one
        # wrong verdict; a stale worker on a corpus writer costs 2,122 captures indistinguishable from
        # current ones for ever.
        #
        # `worker` is REQUIRED rather than defaulted. `capture-check.mjs`'s own header records that a
        # mistyped `--worker=` falls back to IN-PROCESS mode, which on the lab is not a weaker test but a
        # meaningless one — the lab has no NVDA. A default would make that fallback reachable by omission.
        exitMeanings:
          "1": >-
            the capture layer REGRESSED — one or more assertions on the capture RESULT failed. The check
            ran correctly and this is its answer. Read which assertion; do not re-run and hope.
          "2": >-
            it could not run the check at all — no worker reachable, or it refused before testing
            anything. Never read this as "the capture layer is fine".
        # THE ADDRESS IS RESOLVED FROM THE INVENTORY, never handed in. `--worker=http://:8765` cost 29
        # minutes of four capture shards pointed at nothing, and `lab-job.test.ts` enforces that any
        # `--worker=` argv reaches `hostvars[` — resolving a NAME makes a malformed address INEXPRESSIBLE
        # rather than merely rejected. This job's first draft built the URL from the raw `worker` param and
        # that guard caught it, which is the guard doing exactly what the incident bought it for.
        #
        # Spelled out here rather than reusing `lab_named_worker` because `capture-check.mjs` reads only
        # the FLAG (`flagValue(process.argv, "worker")`), never `A11Y_WORKER` — so the `setenv` form the
        # `stability` job uses would resolve correctly and then be ignored, which is worse than not
        # resolving at all.
        argv: "{{ [lab_tsx, 'packages/lab/src/harnesses/capture-check.mjs',
                  '--worker=http://' ~ hostvars[worker].ansible_host ~ ':8765'] }}"
        timeout: 1800

      # `-e only=<family>` NARROWS THE CHECK TO ONE FAMILY, and without it a targeted question could not
      # be dispatched at all. `evidence-check.mjs` has read `--only=` since it was written
      # (`c.family ?? c.id).includes(only)`), the same shape as `--only=` on
      # `capture-screenreader-dataset.mjs`, which `capture-only` exposes — the capability existed here too
      # and no job could reach it. Measured 2026-09-22 on #1908: its acceptance is ten reads of
      # `--only=media-autoplay-audio`, and the only dispatchable form sampled 24 cases at random across
      # the whole corpus, so the family the row asks about might not be in the sample at all. The
      # alternative was a ten-iteration loop typed over `ssh root@<lab>`, which is exactly the shape
      # ADR 0013 exists to forbid.
      #
      # OPTIONAL, unlike `capture-only`'s, and the difference is what an empty value costs. An unnarrowed
      # `capture-only` captures the WHOLE corpus — the four hours that job exists to avoid — while an
      # unnarrowed `evidence-check` is the corpus-wide read this job has always been, and the one every
      # existing caller wants. So the omitted case is the default here and a refusal there.
      #
      # Contained by the SAME shape as `capture-only`'s, `lab_case_id_shape`, for the same reason: it
      # reaches an argv list and never a shell, but the value comes from the wire.
      # `-e worker=<name>` AIMS THE CHECK AT ONE BOX, and without it #1908's ten reads cannot be run at
      # all. Its acceptance is five reads against one worker and five against another — the two-worker
      # form is load-bearing, because #1866's symptom was worker-state-dependent by its own record (the
      # `.good` variant announced "unable to play media." on every capture after a worker's FIRST). This
      # job's only dispatchable form spread the pool, and with `-e only=` narrowing the corpus to ONE case
      # the pool spreads nothing: `drainAcrossPool` drains a shared queue and a case is indivisible, so one
      # item is handled by whichever box wins a `/health` race. Which one that was is not a choice, and
      # until this parameter existed it was not even reportable.
      #
      # OPTIONAL, and the omitted case is the pool: the corpus-wide read across the whole fleet is what
      # every existing caller wants and what this job has always done. Same containment as `stability`'s —
      # `-e worker=` is a NAME from the inventory and the command needs an ADDRESS, resolved through
      # `lab_named_worker`, so `http://:8765` is inexpressible rather than merely rejected.
      evidence-check:
        params: {sample: optional, only: optional, worker: optional}
        # ITS EXIT CODE IS THE ANSWER, not just a status: 0 safe to ship, 1 the evidence CHANGED so a
        # recapture is required, 2 it could not answer. All three are successful runs of the check; only a
        # crash is a failure. They stay non-zero-is-failure deliberately — 1 means "invalidate 2,122
        # cached captures", which must never read as a green job — but the operator is now told which of
        # the three they got instead of having to remember.
        exitMeanings:
          "1": >-
            the evidence CHANGED. The check ran correctly and this is its answer: bump
            CAPTURE_PROTOCOL_VERSION and recapture. Not a fault.
          "2": >-
            INCONCLUSIVE — it could not compare enough captures to answer, which includes PARTIAL
            coverage and not only zero. Re-run it; do not read this as "unchanged".
        # The `--only=` entry is APPENDED OR ABSENT, never rendered empty. `evidence-check.mjs` reads
        # `flag("only", null)` and treats a falsy value as "no filter", so `--only=` would happen to work
        # today — but that is a property of the script's current falsiness, not of this interface, and the
        # list form says what is meant. `is defined` is also the spelling `lab-job.test.ts` derives
        # OPTIONAL from, which is what keeps `params:` above and this argv from disagreeing.
        #
        # THE WORKERS ARE POSITIONAL, so naming one is a narrower LIST rather than a flag: the script
        # takes every positional argument as a worker and pools across them. `[lab_named_worker]` is that
        # list with one entry, which is the same code path a five-box dispatch takes — nothing here tells
        # the script it is being aimed, and nothing in the script has to know.
        argv: "{{ [lab_tsx, 'packages/lab/scripts/evidence-check.mjs']
                  + ([lab_named_worker] if worker is defined else (lab_fleet_workers | split(',')))
                  + ['--sample=' ~ (sample | default(24) | int)]
                  + (['--only=' ~ only] if only is defined else []) }}"
        timeout: 7200

  tasks:
    - name: A job must be named, and it must be one we have
      ansible.builtin.assert:
        that:
          - job is defined
          - job in lab_jobs
        fail_msg: >-
          Pass -e job=<name>, one of: {{ lab_jobs.keys() | list | join(', ') }}.
          There is deliberately no way to pass a command: see this playbook's header.

    # WHICH PARAMETERS THIS JOB READS — DERIVED from its own argv, never declared beside it.
    #
    # Every parameter check below is `when: job == '<name>'`, hand-written, and that shape lets two things
    # through. A new job taking a parameter needs somebody to remember an assert; and a parameter passed
    # to a job that IGNORES it is silently accepted, because Ansible discards an unused extra var without
    # a word. "It did something other than what you asked" must never be silent — the rule
    # `capture-real-pages` already carries for refusing `-e worker=` rather than quietly using five.
    #
    # Both are answerable from the job entry itself, and that is the point: a template saying `{{ only }}`
    # reads that parameter, and one saying `{{ out | default('candidate') }}` reads it and can do without.
    # The requirement is already written in the one place that cannot drift from the command being run.
    # WHAT THIS JOB TAKES — DECLARED AS DATA, PINNED TO THE COMMAND BY A TEST
    #
    # Below this point the parameter checks are `when: job == '<name>'`, hand-written, and that shape lets
    # two things through. A new job taking a parameter needs somebody to remember an assert; and a
    # parameter passed to a job that IGNORES it is silently accepted, because Ansible discards an unused
    # extra var without a word. "It did something other than what you asked" must never be silent — the
    # rule `capture-real-pages` already carries for refusing `-e worker=` rather than quietly using five.
    #
    # The hand-written asserts STAY. They validate the SHAPE of a value — that a shard is `i/n`, that a
    # worker is in the inventory, that an `--only` is ids and not paths. These two answer presence and
    # absence, which a `params:` declaration can.
    #
    # ## Why this is DATA here and a derivation in the test, which is the second design
    #
    # The first version derived it live: re-read this playbook, `regex_findall` the `{{ ... }}` out of the
    # job's argv, and decide from the text. It worked, and it was the wrong place. The SRE Workbook names
    # it exactly — a YAML+Jinja config that grows "ad hoc language features" becomes "an esoteric and
    # complex programming language ... difficult for both humans and tools to maintain and analyze", and
    # its remedy is to separate config from data and put the cleverness in TOOLING. The evidence was
    # already in hand: a `\b` inside a Jinja string is a BACKSPACE (Jinja parses escapes with Python's
    # rules), so the first regex matched nothing, both checks passed vacuously, and it refused `-e only=`
    # on the one job that requires it. A real regex engine with real tests does not have that failure.
    #
    # So the interface is declared beside the command it belongs to, where an operator can read it, and
    # `lab-job.test.ts` DERIVES the same answer from each job's raw argv and refuses any disagreement.
    # That is CLAUDE.md's third remedy for a duplication that cannot be deleted: pin the copies equal.
    - name: What this job takes
      ansible.builtin.set_fact:
        job_params: "{{ lab_jobs[job].params | default({}) }}"

    - name: What this job runs, and what it takes
      # `-e describe=1` answers the question both refusals raise, and is named in both their messages — so
      # it has to exist. An error naming a command that does not is worse than no suggestion.
      #
      # Deliberately BEFORE the refusals. You ask what a job takes precisely when you cannot yet satisfy
      # them, and describing `capture-only` used to hit its own "needs -e only=" first — an answer that
      # withholds itself until you already know it.
      ansible.builtin.debug:
        msg: >-
          {{ job }} — takes {{ job_params | dict2items
             | map(attribute='key') | zip(job_params.values()) | map('join', ': ') | join(', ')
             if job_params else 'no parameters' }}.
          Budget {{ lab_jobs[job].timeout | default('the playbook default') }}s.
          Runs {{ lab_catalogue[job].argv }}
      when: describe | default(false) | bool

    - name: Describing is not running
      ansible.builtin.meta: end_play
      when: describe | default(false) | bool

    # Only for the job that needs it, and only from the inventory. An address cannot be supplied directly,
    # so `--worker=http://:8765` — which cost 29 minutes — is not a value this interface can express.
    - name: Refuse a parameter this job does not read
      # The dangerous half. `-e out=varied` on a job that never reads `out` looks like it worked: Ansible
      # discards the value, the job runs its default, and the operator believes their value was applied.
      ansible.builtin.assert:
        that: item in job_params
        fail_msg: >-
          -e {{ item }}= was passed and job '{{ job }}' does not take it — it takes
          {{ job_params.keys() | list | join(', ') if job_params else 'no parameters at all' }}. Ansible
          would discard the value silently and run the default, so this refuses instead. Drop the
          parameter, or name a job that takes it: `npm run lab:job -- -e job={{ job }} -e describe=1`.
      loop: "{{ lab_caller_params }}"
      when: vars[item] is defined
      loop_control:
        label: "{{ item }}"

    - name: Refuse a MISSING parameter this job cannot run without
      # Without it Ansible renders an empty string, and `capture-only` with an empty `--only=` captures
      # the WHOLE corpus — the four hours that job exists to avoid.
      ansible.builtin.assert:
        that: vars[item.key] is defined
        fail_msg: >-
          job '{{ job }}' needs -e {{ item.key }}=. Its command reads that parameter with no fallback, so
          without it the job runs against an empty value rather than refusing.
          `npm run lab:job -- -e job={{ job }} -e describe=1` lists what it takes.
      loop: "{{ job_params | dict2items | selectattr('value', 'equalto', 'required') | list }}"
      loop_control:
        label: "{{ item.key }}"

    - name: A real-page capture names a role and, if it shards, a well-formed shard
      # `worker` is no longer required, and no longer accepted: the run uses the whole fleet from
      # `A11Y_WORKERS`. It USED to be mandatory and named rather than addressed, so a malformed URL could
      # not be expressed — that guard has moved to where the value now comes from, `inventory.yml` via
      # `configuredWorkers()`, which every other fleet tool already reads.
      #
      # Refused rather than ignored when passed. Accepting `-e worker=` and silently running on all four
      # would be the worst kind of wrong: the operator asked for one machine, watched four get used, and
      # nothing said so. "It did something other than what you asked" must never be silent.
      when: job == 'capture-real-pages'
      ansible.builtin.assert:
        that:
          - worker is not defined
          - role | default('training') in ['training', 'calibration', 'fixture', 'field']
          - shard | default('0/1') is match('^[0-9]+/[0-9]+$')
        fail_msg: >-
          capture-real-pages now runs across the whole fleet ({{ groups['a11y_workers'] | join(', ') }})
          and takes no -e worker. Optionally -e role=training|calibration|fixture|field and -e shard=i/n; sharding is for
          splitting a run across separate machines, not for using more than one worker.

    # Same containment as `worker` and `role`: a NAME from a fixed list, never a path. `--output` is a
    # directory the trainer creates and writes weights into, so an unvalidated value here would be an
    # arbitrary write on the box holding the corpus and the release keys.
    # Same containment as `worker` and `out`: a SHAPE, so a path or a flag is inexpressible rather than
    # rejected. `--only=` reaches an argv list and never a shell, but this value comes from the wire.
    - name: A targeted capture names case ids and nothing else
      when: job == 'capture-only'
      ansible.builtin.assert:
        that:
          - only is defined
          - only is match(lab_case_id_shape)
        fail_msg: >-
          capture-only needs -e only=<case-id> or a comma-separated list, e.g.
          -e only=route-title-stale+ for a whole family, or -e only=<exact-id> for one case. Ids only. Paste what
          check-signals or an audit printed.

    - name: An explain-case job names case ids and nothing else
      when: job == 'explain-case'
      ansible.builtin.assert:
        that:
          - case is defined
          - case is match(lab_case_id_shape)
        fail_msg: >-
          explain-case needs -e case=<case-id> or a comma-separated list, e.g. -e case=acceptance-b3-status-taxi.
          Ids only, with no variant: `scorer:explain --criterion=<X>` prints the list of every miss and false
          alarm on a criterion, ready to paste.

    - name: A train job names its scratch output from a fixed list
      when: job in ['train', 'acceptance', 'shortcuts', 'false-positives', 'explain-case']
      ansible.builtin.assert:
        that: out | default('candidate') in ['candidate', 'multidefect', 'varied', 'scratch']
        fail_msg: >-
          -e out=<name> must be one of: candidate, multidefect, varied, scratch. It is a NAME, not a path — the
          trainer writes weights into `runs/model-<name>`, so a path here would be an arbitrary write.
          Add a name to this list if you need another; do not pass a directory.

    - name: A train that leaves a head out names the group from a fixed list
      when: job == 'train' and exclude is defined
      ansible.builtin.assert:
        that: exclude in lab_train_exclusions
        fail_msg: >-
          -e exclude=<name> must be one of: {{ lab_train_exclusions.keys() | list | join(', ') }}. It is a NAME,
          not a subtype: the subtypes each name stands for are in `lab_train_exclusions`, so a typo cannot
          train a full model under an "excluded" label. Add a name to that map if you need another group.

    # An experiment must never be read as the candidate. `candidate` is what `promote:gated --from=candidate`
    # and the `acceptance`/`sweep` defaults read, so a headless model landing there is one `-e job=acceptance`
    # away from being measured as if it were the model. Explicit, and not `candidate`, or refused.
    - name: A train that leaves a head out names a scratch directory that is not the candidate
      when: job == 'train' and exclude is defined
      ansible.builtin.assert:
        that:
          - out is defined
          - out != 'candidate'
        fail_msg: >-
          -e exclude= needs an explicit -e out=<name> other than 'candidate' (multidefect, varied or scratch).
          A model trained without a head must never land in runs/model-candidate, where every default reader
          would take it for the candidate.

    # Same containment, same reason: a NAME from a fixed list, never a path. This one becomes an env var
    # rather than an argv entry, and an env var reaching a child process is the `A11Y_PYTHON` hazard.
    - name: A sweep names the model it measures from a fixed list
      when: job == 'sweep' and model is defined
      ansible.builtin.assert:
        that: model in ['candidate', 'multidefect', 'varied', 'scratch']
        fail_msg: >-
          -e model=<name> must be one of: candidate, multidefect, varied, scratch. It is a NAME, not a path —
          it becomes `runs/model-<name>`. Omit it entirely to measure the SHIPPED model, which is the default.

    # BOTH stability gates, because both resolve the name through `lab_named_worker` and neither can
    # render without it. `gate-stability` declared `worker: required` and nothing checked the name was one
    # the inventory HAS: the fact below is built `when: worker in groups['a11y_workers']`, so a typo left
    # it unset and the dispatch died on `'lab_named_worker' is undefined` — a message naming the derived
    # fact rather than the parameter the operator mistyped, which sends the reader into this playbook to
    # work out what they got wrong. Same clause, same reason, one task.
    - name: A stability gate names one worker from the inventory
      when: job in ['stability', 'gate-stability']
      ansible.builtin.assert:
        that:
          - worker is defined
          - worker in groups['a11y_workers']
        fail_msg: >-
          {{ job }} needs -e worker=<name from a11y_workers> ({{ groups['a11y_workers'] | join(', ') }}).
          One worker, on purpose: it asks whether the pipeline is deterministic, so a second guest is a
          second variable.

    # Built and then asserted, exactly like `lab_fleet_workers` -- an address that reaches the environment
    # must be proved to be an address, or `--worker=http://:8765` returns by a different door.
    - name: Address the one named worker, for the jobs that take a single guest
      when: worker is defined and worker in (groups['a11y_workers'] | default([]))
      ansible.builtin.set_fact:
        lab_named_worker: "http://{{ hostvars[worker].ansible_host }}:8765"

    # A CAPTURE ROOT IS A NAME, NEVER A PATH — the same containment `capture` and `page` get in
    # `lab-fetch.yml`, and for the same reason: this value reaches a child process as an environment
    # variable, so `../../etc` must be INEXPRESSIBLE rather than rejected later. `dataset-paths.mjs`
    # resolves it under the dataset root, so a name is all it can usefully be.
    #
    # Empty is the normal case and must stay allowed: unset is what every existing caller passes, and
    # `dataset-paths.mjs` already falls back to "captures" on an empty value.
    # THE FACT HAS TO BE BUILT, not just declared. `lab_param_aliases` tells the DERIVATION which
    # parameter produced a fact; it does not create one. Referencing `lab_capture_root` in `setenv`
    # without this task fails at render with `'lab_capture_root' is undefined` — caught by a ONE-CASE
    # probe before a 26-minute two-arm run, which is this repo's own rule about proving a condition can
    # be true before backgrounding anything that waits on it.
    - name: Which captures directory this job writes to
      ansible.builtin.set_fact:
        lab_capture_root: "{{ capture_root | default('') }}"

    - name: A capture root must be a name, so a path cannot be expressed
      when: lab_capture_root | default('') | length > 0
      ansible.builtin.assert:
        that:
          - (lab_capture_root | regex_findall('^[A-Za-z0-9_-]+$')) | length > 0
        fail_msg: >-
          -e capture_root={{ lab_capture_root | truncate(60) }} is not a NAME. It is a directory name
          under the dataset root (letters, digits, underscore, dash), never a path -- e.g.
          `-e capture_root=captures-arm-ten`.

    - name: That address must be exactly an address
      when: lab_named_worker is defined
      ansible.builtin.assert:
        that:
          - lab_named_worker is match('^http://[0-9.]+:8765$')
        fail_msg: >-
          Refusing to run: A11Y_WORKER would be {{ lab_named_worker | truncate(80) }}, which is not a worker
          address. Something has overridden the inventory.

    # `only` is OPTIONAL here, so the shape is asserted only when one was supplied — `only is not defined`
    # is the corpus-wide read every existing caller already gets, not a missing argument. The same shape as
    # `capture-only`'s above, from the same variable.
    - name: An evidence check names a sample size that is a number, and a family that is a name
      when: job == 'evidence-check'
      ansible.builtin.assert:
        that:
          - (sample | default(24)) | int > 0
          - (sample | default(24)) | int <= 200
          - only is not defined or only is match(lab_case_id_shape)
          # A NAME FROM THE INVENTORY, asserted HERE as well as addressed above, because the address is
          # built by a task guarded on `worker in groups['a11y_workers']` — so a name that is not a worker
          # leaves `lab_named_worker` unset and the argv renders `'lab_named_worker' is undefined` at
          # dispatch. That message names the fact rather than the parameter, which sends the reader into
          # this playbook to work out what they mistyped.
          - worker is not defined or worker in groups['a11y_workers']
        fail_msg: >-
          evidence-check takes optionally -e sample=<1..200>, -e only=<family-or-case-id> and
          -e worker=<name from a11y_workers> ({{ groups['a11y_workers'] | join(', ') }}), e.g.
          -e only=media-autoplay-audio to read one family rather than a sample across the whole corpus,
          and -e worker= to read it on ONE named box rather than wherever the pool happened to put it.
          Ids and inventory names only, never a path or an address. Omit -e worker= and it runs across the
          whole fleet from the inventory, which is asserted to be an address list before it is used.

    # BUILT HERE, from the inventory, and then asserted — not interpolated straight into the env.
    #
    # Extra vars have the highest precedence in Ansible, so a value referenced from the job catalogue is
    # reachable by `-e` even when it looks like it comes from the inventory. The assert is what makes an
    # injected `A11Y_WORKERS` inexpressible rather than merely unlikely: the same shape as `isValidCaptureId`
    # and the `--worker` rule above, and the same reason — this env is read by a process that runs as root
    # on the box holding the corpus.
    - name: Address every worker in the fleet, for the jobs that pool
      ansible.builtin.set_fact:
        lab_fleet_workers: >-
          {{ groups['a11y_workers']
             | map('extract', hostvars, 'ansible_host')
             | map('regex_replace', '^(.*)$', 'http://\1:8765')
             | join(',') }}

    - name: The fleet address list must be exactly that, and nothing else
      ansible.builtin.assert:
        that:
          - lab_fleet_workers is match('^http://[0-9.]+:8765(,http://[0-9.]+:8765)*$')
        fail_msg: >-
          Refusing to run: A11Y_WORKERS would be {{ lab_fleet_workers | truncate(120) }}, which is not a
          plain comma-separated list of worker addresses. Something has overridden the inventory.

    # `-e workers=N` MUST NAME A POOL THE FLEET CAN ACTUALLY GIVE, and until this assert existed it did
    # not have to. The cap is applied inside `capture-only`'s `setenv` — `(lab_fleet_workers |
    # split(','))[:workers | default(99) | int]` — which is rendered at dispatch and never goes through
    # the address assert above, so nothing looked at the number at all.
    #
    # MEASURED against a five-host fixture, by running the shipped expression rather than reading it:
    #
    #   -e workers=2    -> two boxes                          correct
    #   -e workers=99   -> ALL FIVE, silently                  the wrong-population shape: "the five-box
    #                                                          arm" and "however many happened to be
    #                                                          there" are different measurements, and a
    #                                                          slice past the end reports the second as
    #                                                          the first. `default(99)` is a sentinel
    #                                                          meaning "all", so an out-of-range number
    #                                                          is indistinguishable from asking for all.
    #   -e workers=0    -> `A11Y_WORKERS=` — EMPTY             the worse one. An empty worker list is not
    #                                                          a refusal: `leaseWorker` falls back to
    #                                                          discovery and guesses `localhost:8765`, so
    #                                                          a corpus capture dispatches at whatever
    #                                                          answers on the lab rather than at the
    #                                                          fleet. The strict address pattern would
    #                                                          have caught it and never sees this value.
    #
    # WHICH BOXES, by NAME or by COUNT. `-e workers=a11y-worker-2,a11y-worker-5` or `-e workers=5`; with
    # neither it is the whole fleet and this is `lab_fleet_workers` unchanged, so every other job behaves
    # exactly as before.
    #
    # NAMES CAME IN AFTER THE COUNT, on `orchestrator`'s ruling and for #21's reason: which five boxes ran
    # is part of what a scaling measurement states, and "the first five in inventory order" is a choice
    # nobody made. The count survives as sugar.
    #
    # NAMES, NEVER ADDRESSES, and that is the design rather than a preference. The address assert above
    # exists because extra vars have the highest precedence in Ansible, so `-e A11Y_WORKERS=...` is
    # reachable even where it looks inventory-derived, and this env is read by a process running as root
    # on the box holding the corpus. A caller-supplied ADDRESS list is exactly the injection that assert
    # makes inexpressible, arriving through a new door. A caller-supplied NAME is resolved through
    # `hostvars` like every other, so the address still comes from the inventory and the result goes
    # through the identical pattern assert below.
    - name: Which workers this run may use
      ansible.builtin.set_fact:
        lab_selected_hosts: >-
          {{ (workers is not defined) | ternary(
               groups['a11y_workers'],
               (workers | default('') | string is match('^[0-9]+$'))
                 | ternary(groups['a11y_workers'][:workers | default(0) | int],
                           workers | default('') | string | split(',') | map('trim') | select | list)) }}

    # EVERY WAY OF NAMING A POOL THE FLEET CANNOT GIVE, refused by name. MEASURED against a five-host
    # fixture by RUNNING the expressions rather than reading them — `[:n]` on a shorter list is silently
    # the whole list, and a non-numeric or zero value renders an EMPTY `A11Y_WORKERS`, which is not a
    # refusal: with no worker list a run goes to DISCOVERY and guesses localhost:8765, so a corpus capture
    # dispatches at whatever answers on the lab. The strict address pattern would have caught that string
    # and never sees it, because the selection is rendered after it.
    - name: A worker selection must name a pool the fleet can actually give
      when: workers is defined
      ansible.builtin.assert:
        that:
          - lab_selected_hosts | length > 0
          - lab_selected_hosts | difference(groups['a11y_workers']) | length == 0
          - (workers | string is not match('^[0-9]+$')) or (workers | int <= groups['a11y_workers'] | length)
        fail_msg: >-
          Refusing to run: -e workers={{ workers }} against a fleet of
          {{ groups['a11y_workers'] | length }} box(es) ({{ groups['a11y_workers'] | join(', ') }}).
          {{ (lab_selected_hosts | length == 0) | ternary(
               'That selects no workers at all, and it does not fail loudly on its own: it produces an
                EMPTY A11Y_WORKERS, which sends the run to worker DISCOVERY and a guessed localhost:8765
                rather than to the fleet. Zero boxes is not a smaller measurement, it is none.',
               (lab_selected_hosts | difference(groups['a11y_workers']) | length > 0) | ternary(
                 'These are not in the inventory: '
                   ~ (lab_selected_hosts | difference(groups['a11y_workers']) | join(', ')),
                 'That is more boxes than the fleet has. Truncating silently would report a run on
                  ' ~ (groups['a11y_workers'] | length) ~ ' as the one you asked for.')) }}

    - name: Address the selected workers
      ansible.builtin.set_fact:
        lab_selected_workers: >-
          {{ lab_selected_hosts
             | map('extract', hostvars, 'ansible_host')
             | map('regex_replace', '^(.*)$', 'http://\1:8765')
             | join(',') }}

    # THE SAME ASSERT, NOT A WEAKER ONE. A subset built from the inventory is still an address list going
    # into a root process, and a selection path that skipped this check would be the one door left open.
    - name: The selected address list must be exactly that, and nothing else
      ansible.builtin.assert:
        that:
          - lab_selected_workers is match('^http://[0-9.]+:8765(,http://[0-9.]+:8765)*$')
        fail_msg: >-
          Refusing to run: A11Y_WORKERS would be {{ lab_selected_workers | truncate(120) }}, which is not
          a plain comma-separated list of worker addresses. Something has overridden the inventory.

    # STATE THE POPULATION, on every run, not only when a selection was asked for. A measurement that
    # names its boxes when somebody remembered to ask has an optional population, and #21 is about that.
    - name: Say which boxes this run will use
      ansible.builtin.debug:
        msg: >-
          {{ lab_selected_hosts | length }} of {{ groups['a11y_workers'] | length }} worker(s):
          {{ lab_selected_hosts | join(', ') }}

    - name: "Run {{ job }}"
      ansible.builtin.include_tasks: tasks/run-job.yml
      vars:
        job_name: "{{ job }}"
        # `argvAppend` is optional and today only `train` has one: flags whose NUMBER depends on a parameter,
        # which a plain list cannot express and a single templated `argv` string would hide from every reader
        # that indexes `argv`. Concatenated HERE so `tasks/run-job.yml` keeps taking one list.
        job_argv: "{{ lab_jobs[job].argv + (lab_jobs[job].argvAppend | default([])) }}"
        job_setenv: "{{ lab_jobs[job].setenv | default([]) }}"
        job_timeout: "{{ lab_jobs[job].timeout }}"
        # `run-job.yml` has read `job_success_codes` since it was written and NOTHING has ever passed it,
        # so `job_ok` was always `[0]` and no job could declare a non-zero exit that means something. Found
        # by `playbook-variables.test.ts`, which is the whole reason that check exists: the reference
        # carried `| default([])`, so the missing value was an empty list rather than an error.
        #
        # Wired from the job entry, defaulting to none — so behaviour is unchanged today and the interface
        # is real rather than notional. `evidence-check` is the obvious first candidate: its exit codes are
        # a documented contract (0 same, 1 evidence CHANGED, 2 inconclusive) where 1 is a successful,
        # informative run, and as a job it currently reports FAILED for it — conflating "told you to
        # recapture" with "broke". Deliberately not changed here: that is a behaviour decision, and this
        # commit is about the wiring being absent.
        job_success_codes: "{{ lab_jobs[job].successCodes | default([]) }}"
        # What a non-zero code MEANS, when the command documents one. A job that answers in exit codes
        # currently reports `exit 1 (expected one of [0])` and leaves the operator to know that 1 is an
        # ANSWER rather than a crash. Reporting the meaning is strictly better than making 1 a success
        # code, which was the other option and is worse: `evidence:check` exiting 1 means "recapture 2,122
        # captures", and a job that reports OK for that is a green light on the most expensive operation
        # this repo has.
        job_exit_meanings: "{{ lab_jobs[job].exitMeanings | default({}) }}"
