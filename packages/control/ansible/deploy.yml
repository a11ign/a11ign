---
# Update every worker to this checkout, and PROVE it took.
#
#     ansible-playbook deploy.yml
#     ansible-playbook deploy.yml -l a11y-worker-3
#
# ## The hole this closes
#
# `worker:deploy` is `utmctl file push` plus a `utmctl` reboot, keyed on a VM UUID. It has no host
# parameter and fails immediately off macOS, so a bare-metal worker had no code-delivery mechanism except a
# human opening an elevated PowerShell on the box. At one machine that is tedious; at twelve it means the
# fleet drifts, and drift here is not cosmetic — `environmentKey()` puts browser and OS versions in the
# capture cache key, so a split fleet fragments the corpus and, per fleet-consistency.mjs, "the cache merely
# stopped hitting, which reads as ordinary churn rather than as a split fleet."
#
# ## Pull, not push
#
# The guests are git-cloned (see code-version.mjs), so a deploy is a `git pull` on the box rather than
# eighteen file copies from the control plane. That is fewer moving parts and it makes rollback the same
# operation as deployment: check out the ref you want and run this again.
#
# ## Verification does not share a failure mode with the action
#
# Ansible acts over SSH; this verifies over HTTP. That is deliberate and this repo learned it expensively:
# the old advice was to hash-check both sides, but reading the guest's hash went through `utmctl exec` too,
# so when `exec` was broken the check returned EMPTY rather than MISMATCHED — and empty reads as a flaky
# tool rather than a failed deploy. `/health.code` is served on the channel the worker is actually used on,
# which is reachable exactly when it is useful.
- name: Deploy the worker code to the fleet
  hosts: a11y_workers
  gather_facts: false
  # ANY HOST'S REFUSAL ENDS THE PLAY, and without this the refusal below protects one box and splits the
  # fleet — which is the outcome it was written to prevent.
  #
  # Ansible removes a FAILED host from the remaining tasks and carries on with the others. So a per-host
  # `fail` in `pre_tasks` skips the busy worker (no destroyed capture, which is half the point) and lets the
  # other nine deploy and reboot, leaving two `codeVersion`s across the fleet —
  # `assertFleetRunsThisCheckout` then refuses every capture run, which is exactly what the refusal claims
  # to stop. Found 2026-09-05 by an adversarial review of the commit that added the refusal; the guard fired
  # and did not halt, and a text-matching test could not tell the two apart.
  #
  # `serial` does NOT substitute for this. `provision.yml`'s `serial: 1` happens to halt because a batch of
  # one that fails is a batch that is 100% failed — but `provision-role.yml` is documented to be run with
  # `--serial=0` (all at once), where one failure of ten leaves nine to proceed. The property wanted here is
  # "abort on any host", which is what this directive says and what `serial` only implies at batch size 1.
  any_errors_fatal: true
  vars:
    a11y_repo_root: "{{ playbook_dir }}/../../.."

  pre_tasks:
    - name: Work out the code version this checkout expects
      # Computed by the SAME function the worker runs (`codeVersion`, which normalises CRLF), because a
      # second implementation is a second chance for the two sides to disagree. A git-cloned Windows guest
      # checks the files out with CRLF, and a byte-for-byte hasher therefore never matched it: measured
      # 31979b551b7a2cfa against this checkout's 22822b7a3a08969c, which read as "permanently stale".
      #
      # Imported BY PATH, not as `@a11ign/nvda-worker`. The package name needs npm workspace
      # resolution, which would mean 100 MB of node_modules on the fleet-control container -- the box
      # that holds the SSH key able to reconfigure every worker. It does not need a dependency tree, and
      # putting the largest supply-chain surface in the system next to that credential is the coupling
      # this split exists to remove (ADR 0012).
      #
      # Still the ONE hasher: code-version.mjs imports nothing but node stdlib and its sibling
      # worker-files.mjs, so a path import is the same function, and code-version.test.ts still forbids
      # a second copy. Verified identical: d1c98aa032198754 either way.
      ansible.builtin.command:
        cmd: >-
          node --input-type=module -e 'const m = await import("./packages/nvda-worker/src/code-version.mjs");
          process.stdout.write(m.codeVersion(m.workerSourceDir()))'
        chdir: "{{ a11y_repo_root }}"
      delegate_to: localhost
      run_once: true
      changed_when: false
      register: expected_code

    - name: Say which version is being deployed
      ansible.builtin.debug:
        msg: "deploying code version {{ expected_code.stdout }}"
      run_once: true

    - name: Is any worker mid-capture
      # Asked over HTTP from the control plane, deliberately NOT over the SSH connection -- the same
      # question the dispatcher asks, answered the same way, so the two cannot disagree about "busy".
      # Copied in shape from `sleep.yml`, which learned it first, and from `provision-role.yml`, which
      # copied it second. THIS PLAY IS WHERE IT WAS MISSING, and it is the play that reboots.
      ansible.builtin.uri:
        url: "http://{{ ansible_host }}:{{ a11y_port }}/health"
        method: GET
        timeout: 5
      delegate_to: localhost
      register: deploy_busy_check
      failed_when: false
      changed_when: false

    - name: REFUSE to deploy underneath a capture
      # MEASURED COST, 2026-09-05: a `capture-real-pages` run started one minute after a different one
      # finished; the progress file still described the FINISHED run, so the fleet read as free, and a
      # deploy went out three minutes later. Twelve in-flight captures died -- "worker forgot capture
      # <id> after accepting it -- it restarted mid-capture, so the work is gone and the case must be
      # re-issued". Nothing was silently corrupted, because the capture-id store turns a lost result into
      # a NAMED re-issue rather than a mystery, but twelve captures were paid for twice.
      #
      # THE SHAPE IS THIS REPO'S MOST EXPENSIVE ONE: a remedy applied at some call sites and not all.
      # `sleep.yml` refuses (it SKIPS the busy box, because a half-slept fleet is harmless).
      # `provision-role.yml` refuses (it HARD-FAILS, because a half-provisioned fleet splits
      # `provisionRevision`, which is a cache key). `deploy.yml` -- the one play that reboots every guest
      # it touches -- checked nothing at all, and its own header explains at length why it reboots.
      #
      # A HARD FAIL, like `provision-role.yml` and unlike `sleep.yml`, and for a stronger reason than
      # either: a partial deploy leaves the fleet on two `codeVersion`s, which `assertFleetRunsThisCheckout`
      # then refuses at the boundary of every capture entry point. So skipping the busy box does not
      # even leave you a usable fleet -- it leaves you a stale one plus a run whose evidence was destroyed.
      #
      # A box that is not answering is NOT a refusal: it is asleep or mid-reboot, and deploying is how you
      # would bring it back. Only a live worker reporting `busy` blocks. `recover.yml` is the deliberate
      # exception in the other direction -- it EXISTS to kill a worker that is busy and wedged, so it must
      # never grow this check.
      when:
        - deploy_busy_check.json.busy | default(false) | bool
        - not (a11y_force_deploy | default(false) | bool)
      ansible.builtin.fail:
        msg: >-
          {{ inventory_hostname }} is CAPTURING right now. Deploying reboots it, which destroys
          12-520 s of screen-reader work that cannot be resumed, and leaves the fleet on two code
          versions -- which `assertFleetRunsThisCheckout` refuses at the start of every capture run.
          Nothing has been changed. See what is going with `npm run lab:status`, end it deliberately
          with `npm run lab:stop -- -e job=<name>`, then deploy the WHOLE fleet together. If you
          genuinely mean to deploy over a live capture, `-e a11y_force_deploy=true`.

  tasks:
    # A GUEST THAT IS STILL COMING BACK IS NOT AN UNREACHABLE ONE, and this play could not tell them
    # apart. It reboots every guest it deploys to, so running two deploys in succession — or deploying
    # after any other operation that rebooted a box — meets a machine that is up but not yet answering
    # ssh, and Ansible reports UNREACHABLE at the first task. Measured 2026-08-26 across four separate
    # runs: one to four boxes dropped each time, every one of them healthy minutes later, and each
    # failure read as a fleet problem rather than as timing.
    #
    # A Windows guest takes a couple of minutes to come back, so the wait is generous; a box that is
    # genuinely off still fails, just after saying it waited rather than at the first packet.
    - name: Wait for the guest to be reachable at all
      ansible.builtin.wait_for_connection:
        timeout: 300
        sleep: 10

    - name: Fetch and fast-forward the guest checkout
      # --ff-only, so a guest that somebody committed on fails loudly here instead of silently diverging
      # from the fleet and producing evidence under a code version nothing else has.
      #
      # IT DID NOT FAIL LOUDLY, AND THAT COMMENT WAS THE ONLY PLACE THE GUARANTEE EXISTED. PowerShell does
      # not abort on a failed NATIVE command, so a `checkout` that cannot find the branch and a `merge`
      # that cannot fast-forward both carried on to `git rev-parse`, which succeeded — and
      # `changed_when: true` then reported "changed" whatever had happened. Measured 2026-08-26: five
      # guests reported `changed` on every deploy while sitting on `main`, serving 0e91c290cc1f1ea0
      # against a checkout of 1204c829ff371a12, through a restart AND a reboot. The verify step caught
      # the SYMPTOM every time and named the wrong cause — "the files on the box are not the ones you
      # think" — because the step that was supposed to put them there had already reported success.
      #
      # `$LASTEXITCODE` after each git call is what makes the intent real: PowerShell's own
      # `$ErrorActionPreference` does not cover native executables, which is exactly the trap here.
      ansible.windows.win_shell: |
        $ErrorActionPreference = "Stop"
        # `-c core.hooksPath=` ON EVERY GUEST-SIDE GIT CALL. A WORKER IS AN APPLIANCE, and this repo's
        # hooks are written for a developer's machine: `post-checkout` is `#!/usr/bin/env bash` and these
        # guests have no bash, so `git checkout` there exits 127 and the deploy fails permanently.
        #
        # Measured 2026-09-07 on a11y-worker-2, the one box that had completed a full deploy at the commit
        # that added the hook: `/usr/bin/env: 'bash': No such file or directory` / `git checkout failed
        # with 127`. `prepare` runs `install-git-hooks.mjs`, so the guest's own `npm install` -- a step of
        # the deploy -- sets `core.hooksPath` on the guest and ARMS this for the NEXT deploy. One box had
        # got there; the other nine had not, which is the only reason the whole fleet did not fail at once.
        #
        # The fix is here rather than in `install-git-hooks.mjs` because a deploy must not depend on the
        # guest's hook configuration AT ALL: every git operation on a guest is performed BY this playbook,
        # on a checkout nobody authors in, so a repository hook has no work to do there and only failure
        # modes. Disabling them at the call site is unconditional; not installing them is a state that can
        # drift. `guest-git-runs-no-hooks.test.ts` pins it across every playbook that runs git on a guest.
        #
        # The value is a path that does not exist rather than empty: git resolves an EMPTY `core.hooksPath`
        # relative to the working directory, so `post-checkout` would still be found in a checkout root
        # that happened to contain one. A non-existent directory makes every hook unfindable, which is what
        # "no hooks" has to mean.
        function Run($what) { & git -c core.hooksPath=a11y-no-hooks @args; if ($LASTEXITCODE -ne 0) { throw "git $what failed with $LASTEXITCODE" } }
        Run "fetch" fetch --quiet origin
        # A WORKER IS AN APPLIANCE: git is the source of truth there and nothing is authored on a guest,
        # so a modified TRACKED file is dirt and it blocks every fast-forward for ever. Measured
        # 2026-08-26: `packages/scorer/bin/fetch-encoder.mjs` was modified on all five boxes, so every
        # deploy since had aborted its merge and reported success anyway.
        #
        # REPORTED before it is discarded, and only tracked files — the same containment `lab:reset`
        # uses, for the opposite reason. On the LAB a dirty file may be work that exists nowhere else and
        # is refused; on a WORKER it cannot be, so it is named and cleared rather than blocking the fleet.
        # Untracked files are never touched.
        $dirty = git -c core.hooksPath=a11y-no-hooks status --porcelain --untracked-files=no
        if ($dirty) { Write-Output "discarding local modifications on this guest:"; Write-Output $dirty; Run "checkout" checkout -- . }
        Run "checkout" -c advice.detachedHead=false checkout --quiet {{ a11y_git_ref | default('main') }}
        Run "merge" merge --ff-only --quiet {{ a11y_expected_commit | default('origin/' ~ (a11y_git_ref | default('main'))) }}
        git -c core.hooksPath=a11y-no-hooks rev-parse HEAD
      args:
        chdir: "{{ a11y_repo_path }}"
      register: guest_head
      changed_when: true

    - name: The guest is ON the commit it was told to be on
      # ASSERTED, not assumed from the step above having exited 0. Verification that shares a failure mode
      # with the action verifies nothing, and this repo has paid for that rule twice already — reading a
      # guest's hash through the same `exec` that was broken returned EMPTY rather than MISMATCHED.
      ansible.builtin.assert:
        that: guest_head.stdout_lines | last | trim == a11y_expected_commit
        fail_msg: >-
          {{ inventory_hostname }} is on {{ guest_head.stdout_lines | last | trim }}, not
          {{ a11y_expected_commit }}. The fetch reported success, so this is the checkout or the
          fast-forward being refused — usually a guest committed on locally, or the branch does not
          exist there. `git -C {{ a11y_repo_path }} status` on the box says which.

    - name: Install dependencies
      # Unconditional and idempotent. Skipping it when the lockfile looks unchanged is the
      # optimisation that leaves one box on last week's guidepup — and guidepup's version is EVIDENCE,
      # in the capture cache key, because it parses NVDA's speech before this project sees it.
      #
      # `--omit=dev` is GONE, and it is what stopped this playbook working at all. A `prepare` script
      # (then `packages/worker-fleet`'s, now the root's, which runs `npm run build`) needs `typescript`, a
      # devDependency -- so omitting dev dependencies removes the compiler that the install itself then
      # tries to run:
      #
      #   npm error command cmd.exe /d /s /c tsc --build
      #   npm error 'tsc' is not recognized as an internal or external command
      #
      # It failed on all four workers on its first real use. Nobody had seen it because deploying worker
      # code needs a fleet to deploy to, and until this week there was one box that had never been
      # successfully provisioned. pnpm installs devDependencies by default, so nothing here can regress it.
      #
      # This script MATCHES `roles/worker/tasks/nvda.yml`'s exactly, deliberately, and
      # `worker-install-sites-match.test.ts` pins it (#2299): two paths running the same operation with
      # different arguments is how one of them quietly stops working while the other keeps proving nothing
      # is wrong. The reasoning for pnpm, `--frozen-lockfile` and the one-time removal of an npm-made
      # tree is written once, THERE.
      ansible.windows.win_shell: |
        $env:COREPACK_ENABLE_DOWNLOAD_PROMPT = '0'
        if (Test-Path 'node_modules\.package-lock.json') {
          Write-Output 'node_modules was made by npm: removing it once, so nothing npm hoisted survives beside pnpm'
          cmd.exe /d /c rmdir /s /q node_modules
          if (Test-Path 'node_modules') { Write-Output 'could not remove node_modules'; exit 1 }
        }
        corepack pnpm install --frozen-lockfile --prefer-offline
        exit $LASTEXITCODE
      args:
        chdir: "{{ a11y_repo_path }}"
      changed_when: true

    - name: Restart the worker task
      ansible.builtin.import_tasks: tasks/restart-worker.yml

    - name: Confirm the worker is serving THIS checkout
      ansible.builtin.import_tasks: tasks/verify-code.yml

  post_tasks:
    - name: Report
      ansible.builtin.debug:
        msg: >-
          {{ inventory_hostname }} at {{ guest_head.stdout | trim }} serving
          {{ served_code }} (expected {{ expected_code.stdout }})
