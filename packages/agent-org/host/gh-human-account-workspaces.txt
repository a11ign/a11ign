# The gh wrapper (packages/agent-org/host/gh) reads this: a workspace id on a line BY ITSELF below acts as
# the human account. Every other agent workspace gets a11ign-ai-workers. Comment lines never match an id.
# Installed by `npm run host:install` to ~/workers/human-account-workspaces.txt; `npm run host:check` reports
# DIVERGED when the installed copy differs. Chairman's identity ruling, 2026-09-24 (#1950, #2332).
#
# TEMPORARY: removed by the row that moves the decision-holders off the human account.
# w6 ceo
w6
# w2 product-manager
w2
# w5 orchestrator
w5
