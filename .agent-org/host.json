{
  "_what": "a11ign's HOST declaration: the machine's facts, which no project's repository can know (ADR 0040, decision 3; #2620, child 3f of #69). `packages/agent-org/src/host-config.mjs` is the one reader. Keys starting with an underscore are prose and are not read.",
  "_where": "The path is `$AGENT_ORG_HOST`, and until a unit sets it (the shadow-run and extraction rows, 4 and 5) it is this file, beside the project's own declaration. NO UNIT IS RENAMED OR REINSTALLED BY THE ROW THAT ADDED THIS FILE: every value below is the one the units, the routing wrapper and the readers already named.",
  "schema": 1,
  "home": "/home/agent",
  "binDir": "/home/agent/.local/bin",
  "primary": "a11ign",
  "projects": [
    { "id": "a11ign", "checkout": "/home/agent/repos/a11y-witness" }
  ],
  "gh": {
    "workers": "/home/agent/workers",
    "leads": "/home/agent/leads",
    "leadsHeader": [
      "The gh wrapper (packages/agent-org/host/gh) reads this: a workspace id on a line BY ITSELF below acts as",
      "a11ign-ai-leads (write, not admin; its own GraphQL pool). Every other agent workspace gets",
      "a11ign-ai-workers, and NO agent workspace acts as the human account. Comment lines never match an id.",
      "Installed by `npm run host:install` to ~/leads/workspaces.txt; `npm run host:check` reports DIVERGED",
      "when the installed copy differs. Chairman's identity ruling, 2026-09-24 (#1950, #2332, #2333)."
    ],
    "leadsWorkspaces": [
      { "id": "w6", "role": "ceo" },
      { "id": "w2", "role": "product-manager" },
      { "id": "w5", "role": "orchestrator" }
    ]
  }
}
