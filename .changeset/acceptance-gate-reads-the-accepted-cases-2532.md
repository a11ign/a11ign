---
"@a11ign/lab": patch
---

**The acceptance evaluator reads the accepted `4.1.3` false-positive and miss set, so a candidate `ceo` has ruled on can read `passed: true` (#2532).** `passed` was `not failureReasons`, and nothing between the false positives and misses and that line could see the ruling on #2258 (2 false positives, 9 misses, `docs/known-gaps.md` §53), so `promote:gated` refused the candidate and the v19→v20 migration could not close. `packages/lab/src/training/accepted-acceptance-cases.json` is now the ONE list, read by the evaluator and pinned to §53 by `known-gaps-file-facts.test.ts`. A case is covered only at its full key (case id + criterion + subtype, the head that fired for a false positive and the head the case is labelled for for a miss); a tenth miss, a third false positive, the same id under another subtype or criterion, and a page that also fires another head all still fail and are named. The list is a `4.1.3` status-head list: the loader and the test refuse any other entry, and `2.4.6` is not covered. The report records `acceptedFalsePositives` and `acceptedMisses` per criterion and an `acceptedCases` block with the list's hash, reports an entry that no longer errs as `staleAcceptedCases` without failing, and leaves the raw `falsePositive`/`falseNegative` counts, precision, recall, stability and the floors exactly as they were.
