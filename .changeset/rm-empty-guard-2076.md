---
"@a11ign/lab": patch
---

`.claude/rules/agent-practices.md` now carries the empty-guard rule — `rm -f "${D:?}"/*.md` rather than `rm -f $D/*.md` — with the 2026-09-23 measurement, the reason bypass does not reach it, and the general form it is an instance of: when a command is refused for its SHAPE rather than its EFFECT, change the shape. It also states which half each defence buys: quoting saves a BARE variable and buys nothing once a glob is attached, so `:?` is what the glob case needs. `roles-readme.test.ts` pins the principle, the remedy, the general form and the population, with the positive assertions written as functions the mutation test runs against each mutated subject; it carries the detector for the dangerous shape with a fixture table in both directions, computes the emptiness over tracked shell-executed files at run time instead of quoting a grep, and pins the nine real `rm`-through-a-variable call sites by name (#2076).
