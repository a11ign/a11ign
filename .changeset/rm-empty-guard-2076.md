---
"@a11ign/lab": patch
---

`.claude/rules/agent-practices.md` now carries the empty-guard rule — `rm -f "${D:?}"/*.md` rather than `rm -f $D/*.md` — with the 2026-09-23 measurement, the reason bypass does not reach it, and the general form it is an instance of: when a command is refused for its SHAPE rather than its EFFECT, change the shape. `roles-readme.test.ts` pins the principle, the remedy, the general form and the already-clean population, with mutations in both directions (#2076).
