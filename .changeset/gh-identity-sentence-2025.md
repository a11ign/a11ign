---
"@a11ign/lab": patch
---

`.claude/rules/agent-practices.md` no longer tells every session that this host has "exactly one `gh` identity configured — so there is nothing here to switch to". It has two: `~/.config/gh` authenticates as `DanBeckDev` and `GH_CONFIG_DIR=/home/agent/workers/gh` as `a11ign-ai-workers`, which is what `a11ign-work-tick.service` sets — and `/home/agent/.local/bin/gh` is a routing wrapper that already switches between them by `HERDR_WORKSPACE_ID`, so a bare `gh` spends an account the reader did not choose (measured 2026-09-23T17:47Z: `/usr/bin/gh api user` read `DanBeckDev` in the same second the wrapper read `a11ign-ai-workers`). The paragraph now carries the refusal the old ground only implied — do not switch to the other config to get past your own limit, because one export changes who every subsequent write is attributed to and that disposition is `ceo`'s — and tells the reader to name the account with `gh api user --jq .login` before reading any pool. `roles-readme.test.ts` pins the refusal, its ground and the routing, never the COUNT: the positive assertions are functions the mutation table runs against each mutated subject, and the direction that matters — the prohibition flipped to its opposite — is the one a guard grepping for `GH_CONFIG_DIR` would survive (#2025).
