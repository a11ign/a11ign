---
"@a11ign/lab": patch
---

`.claude/rules/agent-practices.md`'s "`main` REQUIRES an approving review" section described one of the two surfaces that now carry the requirement. `ceo` added a `pull_request` rule with `required_approving_review_count: 1` to the `merge-queue-main` ruleset (id `23681721`) on 2026-09-23 at ~09:04Z — **ADD, not SWAP**, because requirements compose and exemptions do not, so classic `branches/main/protection` stays and stays authoritative as the only surface whose exemption list can be ENUMERATED. The section now names both surfaces, says which instrument reads each (`A11Y_CHECK_BRANCH_PROTECTION=1` with admin; `rules/branches/main` plus `rulesets/{id}` behind `A11Y_CHECK_MAIN_RULESET=1` without), and states the bound that makes the cheap read usable without becoming an overclaim: `current_user_can_bypass: "never"` answers for the asking identity ALONE and does not mean nobody is exempt, `bypass_actors` is withheld rather than empty on a token without write access to the ruleset, and `CANNOT_TELL` stands unchanged as the verdict for *is anyone else exempt*. `roles-readme.test.ts` pins the bounded CLAIM rather than the field NAME: the positive assertions are functions the mutation table runs against each mutated subject under `assert.throws`, control first, and a control test demonstrates that a guard grepping for `current_user_can_bypass` is green on the inverted file (#2093).
