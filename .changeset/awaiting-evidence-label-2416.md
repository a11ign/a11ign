---
"@a11ign/agent-org": patch
---

**A PR labelled `awaiting-evidence` earns no reviewer order and no author prompt (#2416, `ceo` #928 ruling 2).** A pull request whose done-when needs an external run (a Windows worker, a release dry run) was reviewed before the evidence existed, so the refusal was a foregone conclusion and cost a round (#2393, #2366). `work-gate.mjs` now yields no `draft-awaiting-verdict` for a labelled pull request, reads no commit chain for it, and drops it from `pr-review-blocked`'s AWAITING_REVIEW half; a red build, a refusal and a verdict somebody prompted by hand are still acted on. The rules-file sentence about the author prompting its reviewer says the same, by replacing words (`prefix-budget.test.ts`: 19,994 of 20,000 bytes). A labelled pull request with NO comment after the label was applied and more than 48 hours old yields ONE `awaiting-evidence-stale` order to `product-manager`, naming the pull requests, the label's age and what would clear it; the age comes from the last `labeled` event, one REST call per labelled pull request and none when no open pull request carries the label.
