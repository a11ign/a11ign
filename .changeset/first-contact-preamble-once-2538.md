---
"@a11ign/agent-org": patch
---

**The first-contact preamble goes on a session's FIRST order only (#2538).** `addressed()` wrapped EVERY delivery in the "You are `<session>`" opener, the engineer-brief line, "Work autonomously" and the "ENDING YOUR TURN" paragraph (~1,640 chars), which was right while every delivery followed a `/clear`; since #2483 a per-row instance is never cleared, so `worker-2443`'s second order carried a second copy into a window that already held the first. `deliver` and `clearThenPrompt` now pass `followUp` when the target was neither started this tick nor cleared before the order (a per-row instance mid-row, or a resume, #2470), and `addressed` sends `You are \`<session>\` -- a follow-up order to your session: ...` and the order, keeping the words `token-audit`'s `sessionOf` reads. A started process, a cleared standing seat and any caller that says nothing keep the full preamble; a process started for a resume is briefed in full. Measured on `worker-2443`'s recorded second order: 2,537 chars with all three phrases once, and 1,142 chars with none through the new path. Three gate causes lose the closing sentence the preamble already says (`pr-checks-failing`, `pr-merge-conflict`, `verdict-not-convinced`).
