---
"@a11ign/lab": patch
---

**`calibrate-abstention.mjs` now prints, and stores, the capture-protocol census of the captures it fitted on** (`{"21": 49}`, the shape `evaluate-screenreader-acceptance.py` already reports), so a threshold fitted on a stale calibration split says so in its own output. The 49 calibration captures sat at protocol 18 while the workers served 21, and the fit looked identical to a current one for three meaning bumps (#2212).
